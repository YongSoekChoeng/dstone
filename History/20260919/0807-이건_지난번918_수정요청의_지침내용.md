# 이건_지난번918_수정요청의_지침내용

> 생성일시: 2026-09-19 08:07

## 질문

이건 지난번(9-18) 수정요청의 지침내용이야. 그런데 반영되지 않은 것 같아.

3. 리소스 정의 - 아래와 비슷한 구조를 취하되 더 나은 방향이 있으면 권해줘.
    3-1. 워크플로우
        ...
        resources/ai/agents - Agent 작업단위와 같다. 파일명이 ID. 프롬프트가 여기에 들어간다.

내가 원했던 것은 resources/agents 아래에  agent명.yml 단위로 agent가 쪼개져서 관리되었으면 하는 거였어.

---

## 답변

No tests reference the resource file. Let me check the SqlSyntaxTool comment referencing `agents/sql-convert.yml` — I'll need to update that too. Let's check `docs/09` more broadly for other agent-file mentions, then make the changes.

---

## 답변

Now I'll make the changes. First, the loader — switch agents from a multi-item list-per-file to one-agent-per-file, matching the workflow/mcpServer pattern.

---

## 답변

Good, this looks consistent now. Next, update the AgentRegistry's duplicate-name error message reference (it currently says "agents/*.yml 항목") — still accurate, no change needed there. Now let's rewrite the resource YAML files: split into one-file-per-agent.

---

## 답변

Now split `sql-convert.yml` into four per-agent files:

---

## 답변

Now remove the old grouped file:

---

## 답변

Now update the stray comment reference in `SqlSyntaxTool.java`:

---

## 답변

Now update the docs authoring guide section (line 312–325):

---

## 답변

Now let's verify everything compiles.

---

## 답변

Build succeeded silently (no errors). Let's confirm the jar was produced and quickly sanity-check the startup log line format by grepping for the loader class in the packaged jar (or just trust the successful compile/test-free package).

---

## 답변

All clean, and the build succeeded. Summary of what changed to actually reflect your 9/18 request:

**Behavior change** (`YamlDefinitionLoader.java` + `AgentRegistry`): `agents/*.yml` used to allow a list of multiple agents per file (`agents: [...]`). It now requires exactly one agent per file, top-level key `agent:` — the same pattern `workflows/*.yml` and `mcp/*.yml` already used, and the filename is the agent's identity.

**Resource split**: `sql-convert.yml` (4 agents bundled together) is gone, replaced by:
- `sql-analysis-agent.yml`
- `sql-conversion-agent.yml`
- `sql-fix-agent.yml`
- `sql-conversion-report.yml`

`general-chat.yml` was already 1:1, just converted to the new `agent:` (singular) structure.

**Docs/comments updated**: `docs/09.dstone-ai-engine.md` §9 YAML authoring guide, plus the stray `sql-convert.yml` reference in `SqlSyntaxTool.java`'s javadoc.

`dstone-ai-engine` builds cleanly with this change (`mvn clean package -DskipTests` succeeded).

---

## 질문

기억해줘. 앞으로 진행내용은 영어가 아닌 한글로 보여줘.

---

## 답변

기억했습니다. 앞으로 진행 상황과 답변은 한글로 안내드리겠습니다.
