# localcommandcaveatCa

> 생성일시: 2026-09-18 17:56

## 질문

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

---

## 질문

<command-name>/clear</command-name>
            <command-message>clear</command-message>
            <command-args></command-args>

---

## 질문

지금 dstone-ai-engine 에 대한 프로그램구조를 바꾸고 싶어.
기존 소스는 모두 무시하고 새로 작성한다는 마음으로 과감한 구조변경을 하고 싶어.
아래의 내용을 참고해서 계획을 만들어줘.

<공통지침>
- 기존 소스가 진행계획에 있어서 방해가 될 경우 억지로 우회하지 않고 과감하게 포기한다.
- 모든 소스가 및 구조가 직관적으로 명확해야 한다.
- 모든 소스의 주석이 쉽고 직관적이고 명확해야 하며 자세해야 한다.
- 소스 명명이 전반적으로 통일성을 갖춰야 한다.
- AI 프로바이더 및 모델을 설정

<지침사항>
1. 작업단위정의
    - WorkFlow : 최상위 구조. 여러개의 Step들로 구성.
        - Step : 개별 단위작업. (Agent/Tool)
            - Agent : LLM 을 호출.
            - Tool : 내부/외부 프로그램을 호출.
        - Step이 진행될 때 경우에 따라서는 휴먼이 개입해야 하는 단계가 있을 수도 있는데 이 부분은 어떻게 설계해야 할지 고민해줘.
          (예: Step1 -> Step2 -> 승인자 승인 -> Step3)
          이 경우 Step마다의 Input, Output 등이 일시적으로 저장되었다가 승인자가 승인을 한 후에야 후속 Step으로 넘어가야 할텐데 이런 경우.
        - Step 별 Input, Output 의 정의를 명확하게 해줘.
            
2. 패키지정의 - 아래와 비슷한 구조를 취하되 더 나은 방향이 있으면 권해줘.
    net.dstone.ai
        api - 외부와 통신하는 패키지 
        common - 코어가 되는 컴퍼넌트 패키지
            config - 설정관련 패키지
            consts - 고정값 패키지
            definition - 정의 패키지
            ...
        runtime - 런타임 엔진 패키지 
        tools - 툴 패키지
        ...
3. 리소스 정의 - 아래와 비슷한 구조를 취하되 더 나은 방향이 있으면 권해줘.
    3-1. 워크플로우
        resources/ai/workflows - WorkFlow 작업단위와 같다. 파일명이 ID. 내부적으로 Step 들로 이루어져 있고 각 Step 간의 플로우제어가 이 파일에서 가능해야 한다. 
        resources/ai/agents - Agent 작업단위와 같다. 파일명이 ID. 프롬프트가 여기에 들어간다.
        ...
    3-2. 기타 리소스들 
        resources/schema

4. RAG를 붙일 계획인데 기본적인 구조 및 체인 설계를 해줘.
5. tools 은 내부 java 프로그램, 내부 기타 프로그램(Python, Shell, HttpCall 등)에 대한 기본적인 골격을 갖춘 내용을 채워줘.
6. MCP 서버는 dstone-ai-engine와는 별도로 고민해야 하는 영역인가? 만일 dstone-ai-engine 안에 들어올 수 있다면 함께 고민해줘.

---

## 답변

I've kicked off a survey of the current dstone-ai-engine module (code, YAML schemas, docs, and MCP/HITL search) to ground the redesign. I'll report back once it finishes.

---

## 질문

<task-notification>
<task-id>a6c068ff2a9141ce6</task-id>
<tool-use-id>toolu_01M1jvRujkWUD6KkyQAq6P7w</tool-use-id>
<output-file>/tmp/claude-1000/-app-dstone/a1eee9c2-e792-41bf-a87f-0ccf895a7f36/tasks/a6c068ff2a9141ce6.output</output-file>
<status>completed</status>
<summary>Agent "Survey dstone-ai-engine current architecture" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>I now have complete visibility into the module. Here is the full research report.

## 1. `pom.xml` (`/app/dstone/dstone-ai-engine/pom.xml`)

- Parent: `net.dstone:dstone` (1.0.0-SNAPSHOT), Java 21, log4j 2.24.3
- **Spring AI version: `2.0.1`** (via `spring-ai-bom`), aligned to Spring Boot 4.x / Spring Framework 7
- Dependency override: `commons-io` forced to `2.20.0` (conflict between dstone-common's older commons-io and spring-ai-tika-document-reader's commons-compress requiring `ChecksumInputStream` from 2.16.0+)
- Key dependencies:
  - `net.dstone:dstone-common` (brings web/security/log4j2/redis/kafka/amqp/datasource transitively)
  - `spring-boot-starter-actuator` (k8s probes), logging excluded
  - **Chat model starters (all three on classpath simultaneously, selected at runtime via `spring.ai.model.chat`)**: `spring-ai-starter-model-anthropic`, `spring-ai-starter-model-openai` (also reused for local vLLM via base-url override), `spring-ai-starter-model-ollama`
  - **Vector store**: `spring-ai-starter-vector-store-pgvector` + `spring-ai-vector-store-advisor` (QuestionAnswerAdvisor)
  - **Document ingestion**: `spring-ai-tika-document-reader` (PDF/DOCX/PPTX/HTML/TXT)
  - Lombok (provided), `spring-boot-starter-test`, `spring-ai-spring-boot-testcontainers` (test)
  - **No MCP dependency of any kind** (no `spring-ai-mcp-*`, no `spring-ai-starter-mcp-*`)
  - Note: azure-openai explicitly called out as removed as a chat provider in Spring AI 2.x (comment says it survives only as a vector-store option, but no azure dependency is actually declared here)
- Build: `spring-boot-maven-plugin` (main class `net.dstone.ai.DstoneAiEngineApplication`), resources include `conf/*.properties`, `src/main/java/**/*.xml`, and filtered `src/main/resources/**` (excluding `.java`)

## 2. `application.yml` (`conf/application.yml` — only one found; no `src/main/resources/application.yml` exists)

- `server.port: 8081`
- `management.endpoints.web.exposure.include: health` (k8s probes enabled)
- `spring.http.clients.read-timeout: 5m` (long timeout needed for local Ollama)
- `spring.data.redis`: `enabled: true`, host/port from env vars, timeout 10000ms — used for session, async job status, rate limiting
- `spring.datasource`: PostgreSQL via `log4jdbc`, db `dstone_ai`, password `ENC(...)` (Jasypt), Hikari pool size 10 — used by pgvector
- `spring.servlet.multipart`: max file/request size 20MB (RAG document upload)
- `spring.autoconfigure.exclude`: all Spring Security autoconfig classes + RabbitMQ + Kafka autoconfig (module rolls its own lightweight API-key filter instead of Spring Security)
- `spring.ai.model`: `chat: anthropic` (anthropic|openai|ollama, must be explicit), `embedding: ollama` (RAG only; Anthropic has no embeddings), audio/image/moderation all explicitly `none`
- `spring.ai.anthropic`: `api-key: ENC(...)`, `chat.options.model: claude-sonnet-4-5-20250929`
- `spring.ai.openai`: commented out (template for local vLLM via base-url override)
- `spring.ai.ollama`: `base-url: http://localhost:11434`, `init.pull-model-strategy: when_missing`, `chat.options.model: qwen2.5-coder:3b`, `embedding.model: bge-m3`
- `spring.ai.vectorstore.pgvector`: `initialize-schema: true`, `table-name: vector_store`, `dimensions: 1024` (must match bge-m3 output), `index-type: HNSW`, `distance-type: COSINE_DISTANCE`
- `dstone.ai.session`: `max-messages: 20`, `ttl-seconds: 86400`
- `dstone.ai.prompt`: `default-version: v1`, per-template version overrides for `sample-system`, `oracle-sql-analyzer`, `oracle-to-postgresql`, `oracle-sql-reviewer`, `oracle-sql-fixer` (all `v1`)
- `dstone.ai.tool.allowed-by-caller` — commented-out example (caller → tool whitelist)
- `dstone.ai.rag`: `enabled: true`, `ingest.chunk-size: 800`, `retrieval.top-k: 5`, `retrieval.similarity-threshold: 0.35` (comment: 0.5 default was too strict for bge-m3 in practice)
- `dstone.ai.security.auth`: `enabled: false` (commented `keys: [{key, caller}]` example)
- `dstone.ai.security.ratelimit`: `enabled: false` (commented window/limit/overrides example)
- No tool sub-config (`dstone.ai.tool.shell/python/http.*`) is actually set here — those are empty/absent, meaning shell/python/http tools are effectively disabled by default (fail-closed whitelist design).

## 3. Java files by package

**`net.dstone.ai` (root)**
- `DstoneAiEngineApplication.java` — `@SpringBootApplication`, `@ComponentScan("net.dstone.ai")`; loads `env[-profile].properties` into System properties, then boots with `spring.config.location`/`logging.config` pointed at `APP_CONF_DIR`.

**`api.controller`**
- `ChatController` — `POST /api/ai/chat` (single Agent call) and `/api/ai/chat/stream` (SSE); resolves session/caller, delegates to `AgentRegistry`+`AgentExecutor`; computes the "effective model" for the response.
- `RagController` — `/api/ai/rag/documents` (POST ingest/DELETE), `/api/ai/rag/search` — admin/debug endpoints around `RagService`, always registered regardless of `dstone.ai.rag.enabled`.
- `WorkflowController` — `/api/ai/workflow/{id}/execute` (sync), `/{id}/submit` + `/status/{jobId}` (async via `AsyncJobService`); validates workflowId/caller before dispatching.

**`api.dto`**
- `ChatRequest`/`ChatResponse` — chat contract incl. per-request `ragEnabled`/`toolsEnabled`/`model` overrides.
- `IngestResponse`, `RagSearchRequest`, `RetrievedChunk` — RAG DTOs.
- `WorkflowRequest`/`WorkflowResponse`/`WorkflowSubmitResponse`/`WorkflowStatusResponse` — workflow contracts; status is one of RUNNING/DONE/FAILED with result/error fields.

**`api.service`**
- `AsyncJobService` — submit/status polling for async workflows; `CompletableFuture.runAsync()` on the **default common ForkJoinPool** (explicitly flagged as MVP-only, no dedicated executor/queue/concurrency limit); job state stored as a Redis Hash (`dstone:ai:workflow:job:{jobId}`, 1h TTL). **No pause/resume — a submitted job runs start-to-finish uninterrupted.**

**`common.annotation`**
- `AiTool` — marker annotation (meta-annotated `@Component`) that lets any bean's `@Tool` methods be auto-discovered by `ConfigTool`.

**`common.config`**
- `Config` — `@Import`s all Configuration classes in the module (works around `@ComponentScan` boundary with `dstone-common`).
- `ConfigCallLog` — AspectJ `@Aspect` for method-entry/exit logging across Controller/Service/DAO/runtime/tools packages (honors `@NoAspectLog`).
- `ConfigChatClient` — assembles the single shared `ChatClient` bean: `ChatMemoryRepository` (Redis or in-memory fallback), `ChatMemory` (sliding window via `dstone.ai.session.max-messages`), `List&lt;Advisor&gt;` (currently just `SimpleLoggerAdvisor` + `MessageChatMemoryAdvisor` — explicitly the governance/PII extension point since consumers can add more `@Bean Advisor`s).
- `ConfigRedis` — `RedisTemplate` bean via `dstone-common`'s `RedisUtil`, conditional on `spring.data.redis.enabled=true`.
- `ConfigTool` — scans `@AiTool` beans at `@PostConstruct`, builds a `ToolCallbackProvider`; applies per-caller whitelist (`dstone.ai.tool.allowed-by-caller`), used both for AGENT-step tool-calling and TOOL-step direct lookup (`findByName`).

**`common.consts`**
- `Constants` — all static config-prefix/key constants (AsyncJob keys/TTL, Security auth/ratelimit prefixes, Session Redis key prefix, Tool prefixes, Workflow `DEFAULT_MAX_ITERATIONS=5`/`SUCCESS`/`FAIL` sentinels, Outcome `"실패"` fail-prefix convention, RAG metadata keys, YAML classpath location patterns).

**`common.definition`** (pure records, no logic)
- `AgentDefinition(name, promptName, description, model, toolsEnabled, ragEnabled, allowedCallers)`
- `StepDefinition(id, type, ref, parallelGroup, onFailure, onSuccess, inputTemplate)`
- `StepType` enum — `AGENT` (always succeeds), `RAG` (search-only, no LLM), `TOOL` (deterministic, "실패"-prefix judged), `SUPERVISOR` (LLM judged via structured `Verdict`)
- `WorkFlowDefinition(id, description, maxIterations, allowedCallers, steps)`

**`common.exec`**
- `ExternalProcessRunner` — shared safe process runner for shell/python tools; uses `ProcessBuilder` directly (no shell interpolation), enforces timeout and output-size truncation, "통과:/실패:" convention.

**`common.loader`**
- `YamlDefinitionLoader` — resolves `classpath*:workflows/*.yml` and `classpath*:agents/*.yml` via `PathMatchingResourcePatternResolver`, parses with SnakeYAML, binds to definition records via Jackson `ObjectMapper.convertValue()` (relies on `-parameters` compiler flag).

**`common.prompt`**
- `PromptProperties` — resolves per-template prompt version (`dstone.ai.prompt.versions.{name}`, else `default-version`).
- `PromptTemplateRegistry` — loads/caches `classpath:prompts/{name}/{version}.st` as Spring AI `PromptTemplate`, renders with variables.

**`common.registry`**
- `AgentRegistry` — `@PostConstruct` loads all agents into a `Map&lt;name, AgentDefinition&gt;` (dup names fatal), `resolve(name, caller)` enforces `allowedCallers` whitelist.
- `WorkflowRegistry` — same pattern keyed by workflow `id`.

**`common.security`**
- `ApiKeyAuthFilter` (`@Order(1)`) — `X-API-Key` header check, opt-in via `dstone.ai.security.auth.enabled`; bypasses `/actuator/**`; sets `CallerContext` on success. No Spring Security — module excludes it entirely.
- `RateLimitFilter` (`@Order(2)`) — fixed-window Redis counter per caller/IP, opt-in via `dstone.ai.security.ratelimit.enabled`; throws if enabled without Redis.
- `CallerContext` — static helper storing/reading `caller` as a request attribute (same key reused as ChatClient Advisor context key, though not yet consumed by any advisor).

**`common.session`**
- `RedisChatMemorySession` — implements Spring AI's `ChatMemoryRepository` SPI directly over `RedisTemplate` (no official Spring AI Redis repo exists); one Redis List per conversation (`dstone:ai:session:{conversationId}`) + a `Set` index of conversation IDs; **`TOOL`-type messages throw `IllegalStateException`** on deserialize (tool-calling itself works, but its result-message persistence to Redis is unimplemented).

**`rag`**
- `RagService` — single class handling ingest (Tika extract → `TokenTextSplitter` chunk, or raw JSONL passthrough for `.json/.jsonl` sources) → pgvector upsert-by-`sourceId`; delete; similarity search; and building the `QuestionAnswerAdvisor` used by `AgentExecutor` when `ragEnabled`. Tenant isolation via `tenant` metadata filter keyed on `caller`. Gate: `requireVectorStore()` throws unless `dstone.ai.rag.enabled=true` AND a `VectorStore` bean actually exists.

**`runtime`**
- `WorkflowContext` — mutable `Map&lt;String,Object&gt;` shared across steps (`input`, `result`, `variables` by convention).
- `WorkflowExecutor` — the state machine driving sequential/branch/parallel/loop execution; enforces `maxIterations`; resolves `parallelGroup`s via `CompletableFuture`; success/failure transition logic (`SUCCESS`/`FAIL` sentinels, loop-vs-next by step index comparison).
- `StepFlow`, `StepStatus` (`NEXT_STEP`/`LOOP`/`SUCCESS`/`FAIL`), `StepOutcome(success, text)`, `Verdict(pass, reason)` — supporting value types for the state machine and SUPERVISOR structured judgment.

**`runtime.agent`**
- `AgentExecutor` — the single place that assembles a `ChatClient` request from an `AgentDefinition` (session-id advisor param, system prompt render, conditional RAG advisor, conditional tools, model override, caller advisor placeholder marked `TO-DO`). Exposes `call()`, `stream()`, `callForVerdict()` (uses `.entity(Verdict.class)` for structured SUPERVISOR output).

**`runtime.step`**
- `AgentStepRunner` — runs AGENT (always success) and SUPERVISOR (fail-closed on `Verdict.pass()==false` or parse exception) steps.
- `ToolStepRunner` — runs TOOL steps: strips LLM artifacts (code fences, `[label]` prefixes) from previous-step text, renders `inputTemplate` (`{previous}`/`{varName}` substitution with JSON-escaping), calls `ToolExecutor`, judges success by `"실패"` prefix.
- `RagStepRunner` — pure search step, concatenates retrieved chunk texts, no LLM call.

**`runtime.tool`**
- `ToolExecutor` — single choke point for LLM-free TOOL-step invocation; unwraps Spring AI's JSON-wrapped string tool result before prefix-checking; explicitly called out as the governance/audit extension point.

**`tools.*`**
- `tools.sample.DateTimeTools` — sample `getCurrentDateTime()` tool.
- `tools.sql.SqlSyntaxTools` — `validateSqlSyntax()` using JSQLParser (pure grammar check, no live DB); always active (no whitelist needed).
- `tools.shell.ShellExecTool` / `tools.python.PythonExecTool` — whitelist-only script execution (`allowed-commands`/`allowed-scripts` config maps name→path); disabled by default since whitelist is empty.
- `tools.http.HttpCallTool` — whitelist-only HTTP GET (`allowed-hosts`); disabled by default.

## 4. Workflow/Agent YAML files (verbatim, already captured above in full) — summary of schema

**Agent schema** (`agents/*.yml`, top-level `agents:` list, multiple agents per file allowed):
```yaml
agents:
  - name: &lt;string&gt;
    description: &lt;string&gt;       # doc-only
    promptName: &lt;string&gt;        # -&gt; classpath:prompts/{promptName}/{version}.st
    toolsEnabled: &lt;bool&gt;
    ragEnabled: &lt;bool&gt;
    # model: &lt;string&gt;           # optional override
    # allowedCallers: [&lt;string&gt;]# optional whitelist
```
Files: `general-chat.yml` (1 agent), `sql-convert.yml` (4 agents: sql-analysis-agent, sql-conversion-agent, sql-fix-agent, sql-conversion-report).

**Workflow schema** (`workflows/*.yml`, one workflow per file, top-level `workflow:` key):
```yaml
workflow:
  id: &lt;string&gt;
  description: &lt;string&gt;
  maxIterations: &lt;int&gt;
  steps:
    - id: &lt;string&gt;
      type: AGENT|TOOL|RAG|SUPERVISOR
      ref: &lt;agent-or-tool-name&gt;
      inputTemplate: '&lt;json with {previous}/{varName} tokens&gt;'   # TOOL only
      parallelGroup: &lt;string&gt;          # optional, groups concurrent steps
      onSuccess: &lt;step-id&gt;|SUCCESS
      onFailure: &lt;step-id&gt;|FAIL
```
Files: `oracle-to-postgresql.yml` (analyze→convert→validate↔fix loop→report/SUPERVISOR gate) and `parallel-test.yml` (two TOOL steps sharing `parallelGroup: check`).

No `allowedCallers` field is populated in any current YAML instance (all effectively open to any caller).

## 5. `docs/09.dstone-ai-engine.md` — structure and key details

Doc header: dated 2026-09-15, states this is a **complete from-scratch rewrite** — all prior Phase 0–10 (capability/process-based) code was deleted; history is only in pre-rewrite git log.

Sections (14 total): 1 개요(Overview) · 2 설계 원칙(Design principles) · 3 패키지 구조 · 4 Definition→Runtime 흐름 · 5 Workflow 실행 모델 · 6 Agent와 Tool · 7 Session/RAG/Security · 8 Governance 확장 지점 · 9 YAML 정의 작성법 · 10 API 레퍼런스 · 11 설정 레퍼런스 · 12 빌드 및 실행 · 13 실제 검증 기록 · 14 옛 구조에서 달라진 점.

Key architectural points relevant to your questions:
- **Human-in-the-loop / approval**: Not mentioned anywhere in the doc. SUPERVISOR step is an *LLM* self-review gate (structured Verdict pass/reason), not a human approval gate — it is explicitly contrasted with the mechanical `validate`→`fix` loop as "a judgment call a human should review" but the doc states it still does NOT loop back automatically; a failed SUPERVISOR simply fails the whole workflow. There is no pause point where a human is asked to approve before continuing.
- **RAG chain design**: Tika→TokenTextSplitter→pgvector ingest; `QuestionAnswerAdvisor` with a custom prompt template (deliberately not the default "say I don't know" template, since RAG here is supplementary reference material for agents that already have domain rules in their system prompt); tenant isolation via `tenant` metadata filter derived from `caller`.
- **MCP server**: Not mentioned at all in the doc (0 hits).
- **Tool whitelisting**: Documented in §6 and §11 — `dstone.ai.tool.allowed-by-caller` (caller→tool-name list, empty = allow all), plus separate danger-tool whitelists `dstone.ai.tool.shell.allowed-commands` / `.python.allowed-scripts` / `.http.allowed-hosts` (empty = fully disabled, "fail-closed by omission" design pattern, no separate enable flag).
- **Async job execution**: §10 documents `POST /{id}/submit` → `{jobId}` and `GET /status/{jobId}` → `{jobId, status, result, error}` with status RUNNING/DONE/FAILED; job Hash key `dstone:ai:workflow:job:{jobId}`, 1h TTL. §13 confirms this was manually verified end-to-end (RUNNING→DONE transition, result matches sync execution). Matches code exactly — no discrepancy.
- §8 Governance extension points: (1) `ConfigChatClient`'s `List&lt;Advisor&gt;` injection point for future PII/guardrail advisors, (2) `ToolExecutor.call()` as the single choke point for auditing/limiting tool calls. Both are unimplemented today — pure extension seams.
- §14 explicitly maps old→new concepts (capability deleted, process→Workflow, ChatService split into AgentExecutor+WorkflowExecutor, single string `previousResult`→multi-key `WorkflowContext`, `governance` package deleted with only the two extension points surviving).

## 6. Human-in-the-loop / approval / pause / resume search

Grep across `/app/dstone/dstone-ai-engine/src` and `/conf` for `approval|pause|resume|human[_ -]?in[_ -]?the[_ -]?loop|HITL` (case-insensitive): **zero matches**. Confirmed also by reading every runtime/step class: `WorkflowExecutor.run()` executes a workflow to completion (or exception) in one call with no persisted intermediate state and no external signal/callback mechanism. `AsyncJobService` only tracks RUNNING/DONE/FAILED — there is no PENDING_APPROVAL-style state, no API to inject a decision mid-flight, and no persistence of `WorkflowContext` between steps beyond the in-memory object for that single execution. **Conclusion: no HITL/approval/pause/resume mechanism exists anywhere in this module today** — this would be new-build territory for your restructure.

## 7. MCP (Model Context Protocol) search

Repo-wide grep for `\bmcp\b|ModelContextProtocol|spring-ai-mcp` across `*.java|*.xml|*.yml|*.yaml|*.md` (excluding `/target/`): all hits are in `/app/dstone/History/**/*.md` — these are chat/session transcript files (Claude Code conversation history), not source code or config. One transcript (`History/20260918/1313-...md`) even records the user's own question "MCP 서버는 dstone-ai-engine와는 별도로 고민해야 하는 영역인가?" (should MCP server be considered separately from dstone-ai-engine?) — i.e., this is the same design conversation you're in now, not existing implementation. **Conclusion: there is no MCP code, dependency, or config anywhere in the actual codebase (pom.xml, Java, YAML) — it is 100% greenfield.**

## 8. k8s / Dockerfile deployment

Confirmed present, not read in depth per your instruction:
- `/app/dstone/dstone-ai-engine/Dockerfile` (1201 bytes)
- `/app/dstone/dstone-ai-engine/k8s/` containing `namespace.yaml`, `deployment.yaml`, `service.yaml`, `configmap.yaml` (configmap is the largest at ~4.7KB, likely mirrors `application.yml` keys)

---

### Quick synthesis for your restructure decision
The current engine is a clean, recently-rewritten (2026-09-15), fairly minimal 4-layer design (Workflow→Step→Agent→Tool, all YAML-declared) with working sync/async execution, RAG, session memory, and opt-in auth/rate-limit — but it has **zero** HITL/approval infrastructure and **zero** MCP integration; both would need to be designed from scratch. The existing `SUPERVISOR` step type and the `Advisor`/`ToolExecutor` extension seams (§8 of the doc) are the two places a new design would most naturally hook into for governance/approval and tool-call auditing, but neither currently pauses execution or persists state to await external input — `WorkflowExecutor.run()` is a fully synchronous, single-threaded-call state machine with no serialization of `WorkflowContext`.</result>
<usage><subagent_tokens>166017</subagent_tokens><tool_uses>24</tool_uses><duration_ms>153144</duration_ms></usage>
</task-notification>

---

## 답변

Before I finalize the plan, two architectural forks materially change the file layout — I'd rather confirm these than guess.

---

## 질문

[Request interrupted by user for tool use]

---

## 질문

이 계획서를/app/dstone/LOGS 아래에 다운로드 시켜줘

---

## 답변

`/app/dstone/LOGS/dstone-ai-engine-restructure-plan.md`에 계획서를 저장했습니다. 내용 수정이나 추가로 진행할 부분이 있으면 말씀해 주세요.

---

## 질문

Step 유형 에 RAG 가 필요있을까?
RAG는 프롬프트 에 Advising 되는 것이지 별도의 Step 으로 빼내는게 합리적인지 모르겠어.

---

## 답변

Good catch — RAG isn't really an independent "unit of work," it's context injection before an LLM call. Let me rework that section: drop `StepType.RAG` entirely, keep RAG as an Agent capability (`ragEnabled`) for the LLM path, and add a thin `RagSearchTool` for the rare case where you want raw retrieval without invoking an LLM — that way "search" logic lives in exactly one place (`RagRetrievalChain`) instead of two.

---

## 답변

RAG 관련 부분 반영 완료. `StepType.RAG`(및 `RagStepRunner`)를 없애고, RAG는 두 경로로만 존재하도록 정리했습니다:

- **LLM 필요할 때**: `AgentDefinition.ragEnabled: true` → AGENT 스텝에서 `RagRetrievalChain`이 만든 Advisor가 자동으로 프롬프트에 컨텍스트를 꽂아줌 (기존 방식 그대로).
- **LLM 없이 검색 결과만 필요할 때**: 신규 `tools.rag.RagSearchTool` — 그냥 일반 `TOOL` 스텝으로 호출. `SqlSyntaxTool`처럼 무해한 조회 동작이라 화이트리스트 없이 상시 활성.

이렇게 하면 검색 로직이 `RagRetrievalChain` 한 곳에만 있고, Step 유형은 `AGENT/TOOL/SUPERVISOR/APPROVAL` 4가지로 줄어듭니다. §1.1, §3(패키지 트리), §4(리소스), §5(RAG 체인), §8(유지/폐기 표), §9(Phase 1)에 전부 반영했고, `/app/dstone/LOGS/dstone-ai-engine-restructure-plan.md`와 plan 파일 양쪽에 저장 확인했습니다 (참고로 이 파일이 한 번 저장 직후 사라졌다가 재작성 후 정상 확인됨 — LOGS 디렉토리에 로그 정리 프로세스가 있다면 이 문서는 다른 위치로 옮기는 걸 권합니다).

다른 수정할 부분 있을까요?

---

## 질문

dstone/docs/temp 디랙토리에 옮겨줘

---

## 답변

`docs/temp/dstone-ai-engine-restructure-plan.md`로 이동했습니다 (LOGS엔 더 이상 없음). 원본이 git 추적 대상이 아니어서 일반 `mv`로 처리했습니다.

---

## 질문

Step 이 종료되는 경우의 수는 어떻게 되나?

---

## 답변

Step은 두 계층에서 "끝"이 결정됩니다 — **① 개별 러너의 실행 결과**와 **② 그 결과를 받은 `WorkflowExecutor`의 다음 동작 결정**입니다. 지금 계획서엔 이 구분이 암묵적으로만 있어서 명시적으로 정리하겠습니다.

**① 러너 실행 결과 (`StepOutput` 기준) — 4가지**

| 결과 | 의미 | 발생 조건 |
|---|---|---|
| SUCCESS | 정상 성공 | AGENT 정상 응답, TOOL 결과에 `"실패:"` 없음, SUPERVISOR `Verdict.pass()==true` |
| FAILURE | 비즈니스 로직상 실패 | TOOL `"실패:"` 접두어, SUPERVISOR `Verdict.pass()==false` — `success=false, failureReason` 有 |
| ERROR | 처리 불가능한 예외 | LLM API 오류, DB 연결 끊김, MCP 서버 무응답 등 — 러너가 던진 예외를 `WorkflowExecutor`가 캐치 (`StepOutput`으로 안 돌아옴) |
| PENDING | 아직 안 끝남 (`APPROVAL` 전용) | `WorkflowExecutor`가 러너를 아예 호출하지 않고 실행을 중단 |

**② `WorkflowExecutor`의 다음 동작 결정 (`StepStatus`) — 기존 4종 + 신규 2종**

기존 `StepStatus`(`NEXT_STEP`/`LOOP`/`SUCCESS`/`FAIL`)에 `ERROR`, `WAITING_APPROVAL`을 추가해 6가지로 확장이 필요합니다.

| StepStatus | 트리거 | 워크플로우 동작 |
|---|---|---|
| `NEXT_STEP` | SUCCESS/FAILURE + `onSuccess`/`onFailure`가 다른 스텝 id | 그 스텝으로 진행 |
| `LOOP` | `onSuccess`/`onFailure`가 이전/현재 스텝을 다시 가리킴 (validate↔fix) | `maxIterations` 증가 후 재실행, 초과 시 강제 `FAIL` |
| `SUCCESS` | `onSuccess`/`onFailure`가 `"SUCCESS"` sentinel | `DONE`, 워크플로우 정상 종료 |
| `FAIL` | `onSuccess`/`onFailure`가 `"FAIL"` sentinel, 또는 LOOP 초과 | `FAILED`, 워크플로우 종료 |
| **`ERROR`(신규)** | 러너가 예외를 던짐 | `onFailure` 분기를 **무시**하고 즉시 `FAILED`+`errorMessage` 기록 |
| **`WAITING_APPROVAL`(신규)** | `APPROVAL` 스텝 도달 | `WAITING_APPROVAL` 저장 후 중단, `decision` API로 재개 시 승인/반려에 따라 `NEXT_STEP`(→onSuccess/onFailure)으로 재평가 |

핵심 판단은 **ERROR(시스템 예외)와 FAILURE(비즈니스 실패)의 분리**입니다. 기존처럼 TOOL의 `"실패:"` 접두어 하나로만 판정하면, "DB가 죽어서 응답을 못 받은 것"과 "SQL 문법이 틀려서 실패한 것"이 같은 `onFailure` 분기(예: fix-loop)로 흘러가 무한 재시도/오탐이 날 수 있습니다. `ERROR`는 `onFailure`를 거치지 않고 바로 워크플로우를 죽입니다.

계획서에 이 내용을 §1.5로 추가하겠습니다.

---

## 답변

`docs/temp/dstone-ai-engine-restructure-plan.md`에 §1.5 "Step 종료 경우의 수"를 추가했습니다. 다른 부분도 손볼 곳이 있을까요?

---

## 질문

PostgreSQL 스키마의 테이블명을 대문자로 해줘.
그리고 매 Step 이 진행될 때의 IN/OUT 의 로깅을 확실하게 해줘. 어떤 Step이 유형이 뭔데 에이전트아이디가 뭐고 Step 결과는 어떻더라 등이 로그를 보면 한눈에 들어올 수 있도록.
그리고 WorkFlow 의 진행상태 및 내역을 조회할 수 있는 API를 추가해줘.
그리고 dstone-boot 에서 dstone-ai-engine 관련 모든 테스트를 진행 가능하도록 추가해줘.

---

## 답변

1~3번은 계획서에 반영했습니다 (스키마 대문자화, Step IN/OUT 로깅 설계 §1.6, 조회 API §10). 4번 "dstone-boot에서 dstone-ai-engine 관련 모든 테스트 진행 가능하도록" 부분은 구현 형태가 여러 갈래라 확인이 필요합니다 — dstone-boot는 원래 dstone-ai-engine과 코드 의존관계가 없는 별개 모듈입니다 (둘 다 dstone-common만 의존).

---

## 답변

4가지 모두 반영했습니다.

1. **스키마 대문자화** (§1.4): `AI_WORKFLOW_EXECUTION`, `AI_WORKFLOW_EXECUTION_STEP_HISTORY` — 테이블/컬럼명 모두 `dstone-batch`류 대문자 관례로 통일. 겸사겸사 `STEP_REF`/`DURATION_MS`/`FAILURE_REASON` 컬럼을 추가해 로깅 내용과 DB가 같은 정보를 갖게 했습니다.

2. **Step IN/OUT 로깅** (§1.6 신규): `WorkflowExecutor`가 스텝 시작/종료마다 `WF_STEP phase=... executionId=... stepType=... ref=... result=... transition=... durationMs=...` 형태의 logfmt 한 줄 로그를 남기도록 설계 — 기존에 있던 `LOGS/dstone-ai-engine/execution/execution.log`로 라우팅해서 새 파일 없이 활용.

3. **Workflow 진행상태/내역 조회 API** (§10 신규): `GET /executions`(목록, 상태 필터), `GET /executions/{id}`(상세+스텝 히스토리), `POST /executions/{id}/decision`(승인/반려) 3종 엔드포인트로 정리.

4. **dstone-boot 테스트 연동** (§11 신규): 실제로 이미 `net.dstone.boot.ai.*`에 dstone-ai-engine을 호출하는 수동 테스트 화면(Chat/Document/SqlConvert/**WorkflowTest**)이 있길래 확인해서, 새로 만들지 않고 기존 `WorkflowTestController`/`WorkflowTestService`를 새 API 계약(`jobId`→`executionId`, `/status`→`/executions`)에 맞게 갱신 + 승인 처리·실행목록/이력 조회 화면을 추가하는 걸로 설계했습니다.

Phase 순서(§9)와 검증 방법에도 각 항목을 반영해뒀습니다. 더 손볼 곳 있으면 말씀해주세요.

---

## 질문

1. runtime.WorkFlowAuditLogger 대신 ConfigCallLog 에서 기능을 넣을 수 없나?
2. net.dstone.ai.api.controller.RagController, net.dstone.ai.rag.RagService ========================>>> 이거 필요없지 않아?
3. resources/prompts/{name}/{version}.st 를 제거하고 싶어. 그보다는 resources/agents/{에이전트아이디}.yml 안에 프롬프트항목을 넣고. 그렇다면 net.dstone.ai.common.prompt 는 필요없지 않을까?
4. runtime 하위 패키지 구성을 아래와 같이 해줘.
    ├── runtime
        ├── workflow
        │   ├── WorkFlowContext, WorkFlowExecutor
        │   └── execution **신규 패키지**
        │       ├── WorkFlowExecution, StepHistoryEntry
        │       ├── WorkFlowExecutionStore   (JdbcTemplate 영속화)
        │       └── WorkFlowExecutionService (동기/비동기/승인대기 실행의 단일 진입점)
        ├── agent
        │   └── AgentExecutor
        ├── step
        │   └── AgentStepRunner, ToolStepRunner, ApprovalStepRunner
        └── tool           (ToolExecutor — MCP 툴도 동일 경로로 호출되므로 변경 없음)
        │   └── ToolExecutor
        ├── status
            ├── WorkFlowExecutionStatus, StepStatus, StepInput/StepOutput **(신규, StepOutcome 대체)**, Verdict

---

## 답변

Good catches on all four — let me work through each and update the doc.

**1. Logging via `ConfigCallLog` instead of a new `WorkflowAuditLogger` class** — yes, this is better. If every step runner implements one common interface (`StepRunner.run(execution, definition, input)`), `ConfigCallLog` can wrap that one join point with a dedicated `@Around` advice and extract `executionId/stepType/ref/result/durationMs` straight from the typed arguments — no manual log calls scattered through `WorkflowExecutor`. I'll drop `transition=` from the log line since that's decided by `WorkflowExecutor` *after* the join point returns, not something the aspect can see.

**2. `RagController`/`RagService`** — I checked `dstone-boot`'s existing `DocumentController`/`DocumentService`: it calls `POST /api/ai/rag/documents` and `DELETE /api/ai/rag/documents/{sourceId}` directly today. So the ingest/delete part of `RagController` is a real, already-used dependency — can't drop it. But `RagController`'s `/search` action has **no caller anywhere** (not used by dstone-boot, and now redundant with `RagSearchTool` + the Advisor path) — that one I'll remove. `RagService` stays (split per §5) since three different callers (ingest endpoint, Advisor, `RagSearchTool`) all need the same underlying logic in one place.

**3. Inline prompts into `agents/*.yml`, drop `common.prompt`** — agreed, this removes a layer of indirection (agentId → promptName → version → .st file) for a versioning capability nothing currently uses. Git history on the YAML file covers "what did this prompt used to say" just as well.

**4. `runtime` package restructure** — applying your structure as given (keeping `Workflow` spelling to match every existing class/YAML key/endpoint in the codebase — flagging in case you actually wanted `WorkFlow`, but assuming casual typing since it'd conflict with the "명명 통일성" goal otherwise).

Let me update the document.

---

## 답변

Now the logging section (§1.6) — fold into `ConfigCallLog`, drop `transition=`:

---

## 답변

Just one stale reference at line 84 — let's fix that path.

---

## 답변

Structure looks coherent. Summary of what changed:

1. **로깅**: `runtime.WorkflowAuditLogger` 제거, `common.config.ConfigCallLog`에 `StepRunner.run(..)` 전용 `@Around` advice를 추가하는 걸로 바꿨습니다. 모든 러너가 `StepOutput run(WorkflowExecution, StepDefinition, StepInput)` 한 시그니처로 통일되니까 이 join point 하나만 감싸면 됩니다. `transition=` 필드는 로그에서 뺐습니다 (join point 밖, `WorkflowExecutor`에서 나중에 결정되는 값이라 advice가 알 수 없음).

2. **RagController/RagService**: `RagService`는 유지(단, `RagIngestService`+`RagRetrievalChain`으로 분리). `RagController`는 `/documents`(ingest/delete)만 유지 — 실제로 확인해보니 `dstone-boot`의 `DocumentController`가 이 엔드포인트에 의존하고 있었습니다. `/search`는 삭제 — 아무 데서도 안 부르고 있고 Advisor/`RagSearchTool`이 이미 커버합니다.

3. **프롬프트 인라인**: `resources/prompts/`, `common.prompt` 패키지 전부 폐지. `AgentDefinition.promptName` → `prompt`(원문 문자열) 필드로 교체, `agents/*.yml`에 직접 씁니다. 변수 치환은 `AgentExecutor`가 Spring AI `PromptTemplate`을 그 자리에서 직접 써서 유지.

4. **runtime 재구성**: 말씀하신 대로 `workflow`(+`execution` 하위)/`agent`/`step`/`tool`/`status` 5개로 나눴습니다. `ApprovalStepRunner`도 다른 러너와 완전히 동일하게 `StepRunner` 인터페이스로 호출되도록 바꿔서 (기존엔 `WorkflowExecutor`가 APPROVAL을 미리 가로채는 특수 분기가 있었는데 없앴습니다), 재개(resume)는 "같은 스텝을 결정값과 함께 다시 실행"하는 것으로 단순화됐고 이게 1번 로깅 재사용도 가능하게 해준 핵심 변경입니다.

전체 반영 완료했습니다 (`docs/temp/dstone-ai-engine-restructure-plan.md`). 더 볼 곳 있으면 말씀해주세요.

---

## 질문

stop hooks 에 대한 해법은 ?

---

## 답변

"stop hooks"가 정확히 어떤 것을 가리키는지 확인이 필요합니다 — 문맥상 여러 갈래로 해석될 수 있어서요.

---

## 답변

"stop hooks"가 어떤 걸 가리키시는지 구체적으로 알려주시겠어요? 지금 떠오르는 후보는 (1) 진행 중이거나 승인 대기 중인 워크플로우 실행을 취소/중단하는 메커니즘, (2) 배포(K8s pod)가 종료될 때 실행 중이던 워크플로우를 안전하게 처리하는 graceful shutdown, (3) 이 세션/Claude Code 자체의 Stop hook 설정 — 세 가지인데, 다른 의도가 있으면 말씀해주세요.

---

## 질문

이 로그가 보여서 ... 이거 어떻게 해결해? Stop hook error: Failed with non-blocking status code: /bin/sh: 1: /app/dstone/.claude/hooks/save-history.sh: Permission denied

---

## 답변

That's it — the script isn't marked executable (`-rw-r--r--`, no `x` bits), so `/bin/sh` can't invoke it directly.

---

## 답변

`chmod +x`로 실행 권한을 추가했습니다. 다음 Stop 이벤트부터는 이 오류 없이 정상 실행될 겁니다.

참고로 이 스크립트는 git으로 추적되는 파일이면, git이 파일 모드(실행 비트)를 저장하기 때문에 새로 clone/checkout할 때 다시 권한이 풀릴 수 있습니다 — 그런 경우 `git update-index --chmod=+x .claude/hooks/save-history.sh`로 실행 비트를 커밋해두면 재발을 막을 수 있습니다. 확인해드릴까요?

---

## 질문

응 그렇게 해줘

---

## 답변

실행 비트 변경만 커밋했습니다 (`58966ca`). `docs/temp/dstone-ai-engine-restructure-plan.md` 변경분은 요청하신 게 아니라서 건드리지 않고 작업 디렉토리에 그대로 남겨뒀습니다.

---

## 질문

1. 로깅: runtime.WorkflowAuditLogger 제거, common.config.ConfigCallLog에 StepRunner.run(..) 전용 @Around advice를 추가하는 걸로 바꿨습니다.
     모든 러너가 StepOutput run(WorkflowExecution, StepDefinition, StepInput) 한 시그니처로 통일되니까 이 join point 하나만 감싸면 됩니다.
     transition= 필드는 로그에서 뺐습니다 (join point 밖, WorkflowExecutor에서 나중에 결정되는 값이라 advice가 알 수 없음).
    ==>> OK.
    
  2. RagController/RagService: RagService는 유지(단, RagIngestService+RagRetrievalChain으로 분리). RagController는
     /documents(ingest/delete)만 유지 — 실제로 확인해보니 dstone-boot의 DocumentController가 이 엔드포인트에 의존하고 있었습니다. /search는
     삭제 — 아무 데서도 안 부르고 있고 Advisor/RagSearchTool이 이미 커버합니다.
    ==>> Rag 테스트 관련한 dstone-boot 에서의 사항들은 모두 제거해줘.
     
  3. 프롬프트 인라인: resources/prompts/, common.prompt 패키지 전부 폐지. AgentDefinition.promptName → prompt(원문 문자열) 필드로 교체,
     agents/*.yml에 직접 씁니다. 변수 치환은 AgentExecutor가 Spring AI PromptTemplate을 그 자리에서 직접 써서 유지.
    ==>> OK.
     
  4. runtime 재구성: 말씀하신 대로 workflow(+execution 하위)/agent/step/tool/status 5개로 나눴습니다. ApprovalStepRunner도 다른 러너와 완전히
     동일하게 StepRunner 인터페이스로 호출되도록 바꿔서 (기존엔 WorkflowExecutor가 APPROVAL을 미리 가로채는 특수 분기가 있었는데
     없앴습니다), 재개(resume)는 "같은 스텝을 결정값과 함께 다시 실행"하는 것으로 단순화됐고 이게 1번 로깅 재사용도 가능하게 해준 핵심
     변경입니다.
    ==>> OK.

---

## 답변

Now let me update the plan — RagController's ingest/delete justification shouldn't hinge on dstone-boot anymore, and §11 needs a full "delete these files" list.

---

## 답변

Now the main §11 rewrite — removing the "no change needed" claim about `DocumentController` and adding a concrete deletion list based on what I found in the actual `dstone-boot` source tree:

---

## 답변

Now the "no change needed" line further down — remove `DocumentController` from that list since it no longer exists:

---

## 답변

All consistent now — every remaining mention is in the "delete this" context. Summary of what changed:

- **§5**: `RagController`의 `/documents` (ingest/delete) API 존재 이유를 "dstone-boot 의존" → "재사용 가능한 플랫폼이니 API 자체는 필요"로 재정리.
- **§11**: `dstone-boot`의 RAG 문서 테스트 화면(`DocumentController`/`DocumentService`/`DocumentDao`+sqlmap/`DocumentVo`/`IngestResult`/`document.jsp`/`document.js`/index 카드 링크/`TB_AI_DOCUMENT` 테이블)을 전부 삭제 대상으로 명시. Chat/Workflow 테스트 화면은 그대로 유지·갱신.
- **§8, §9, 검증 방법**: 위 삭제를 Phase 5와 체크리스트에 반영.

`docs/temp/dstone-ai-engine-restructure-plan.md`에 전부 반영했습니다. 더 손볼 곳 있을까요?

---

## 질문

아 dstone-boot의 Document* 관련 로직은 embedding 관련 내용 아닌가?
문서 embedding 기능은 살리고 
net.dstone.ai.api.controller.RagController => net.dstone.ai.api.controller.EmbedController 
net.dstone.ai.rag.RagService => net.dstone.ai.api.service.EmbedService
이렇게 바꿔주는게 어떨까?
그리고 이 embedding 내용을 토대로 RAG가 내부적으로 동작하는거 아냐?

---

## 답변

Good call — separating "build the embeddings" from "use the embeddings for RAG" is a real conceptual split, not just a rename. Let me apply it.

---

## 답변

Now revert §11 back to "keep, just update the URL":

---

## 답변

Now the verification section:

---

## 답변

All consistent now. 정확한 통찰이었습니다 — 임베딩(적재)과 RAG(검색·증강)는 별개 관심사였는데 이름 하나(`RagController`/`RagService`)로 뭉쳐 있었네요. 반영한 내용:

- **`RagController` → `EmbedController`**(`api.controller`), **`RagService`의 ingest/delete 부분 → `EmbedService`**(`api.service`로 이동) — `/api/ai/embed/documents`로 엔드포인트도 리네임.
- **`RagService`의 검색 부분은 `rag.RagRetrievalChain`으로 그대로 유지** — 이게 진짜 "RAG"(임베딩을 읽어서 LLM에 증강)이고, `EmbedService`가 만들어둔 결과물을 소비하는 쪽입니다. 말씀하신 대로 RAG는 내부적으로 이 임베딩 위에서 동작하는 구조가 됩니다.
- 지난 턴에 삭제하기로 했던 `dstone-boot`의 `DocumentController`는 **되돌려서 유지** — 이건 애초에 "RAG 테스트 화면"이 아니라 "임베딩 적재 테스트 화면"이었으니 지울 이유가 없었습니다. 호출 URL만 `/api/ai/embed/documents`로 갱신.

§3(패키지 구조), §5(Embedding/RAG 분리 설명), §8(유지·폐기표), §9(Phase 순서), §11(dstone-boot 연동), 검증 방법, Context 결정사항까지 전부 갱신했습니다.

---

## 질문

net.dstone.ai.rag.RagRetrievalChain ==>> net.dstone.ai.common.rag.RagRetrievalChain 로 수정해줘.

---

## 답변

Now update the prose references from `rag.RagRetrievalChain`/`rag.chain` to `common.rag.*`:

---

## 답변

Now the keep/discard table row:

---

## 답변

Clean — those two remaining `net.dstone.ai.rag` mentions are correctly the "moved from" references, and `tools.rag.RagSearchTool` is intentionally untouched (different package). `net.dstone.ai.rag.RagRetrievalChain` → `net.dstone.ai.common.rag.RagRetrievalChain` reflected throughout §3, §5, §8.

---

## 질문

steps 에 parallelGroup 기능은 살아있는 거지 ?

---

## 답변

Good catch — let me check how `parallelGroup` actually works in the current code before I say anything, since my redesign of the step loop (§1.2/§2) never explicitly addressed it.

---

## 답변

parallelGroup은 실제 코드(`runtime.WorkFlowExecutor.runParallel()`)를 보고 왔습니다 — 살아있고, 지금까지 제 계획에 명시적으로 안 담겨 있던 부분이라 이번에 확실히 정리해서 추가하겠습니다. 그런데 이 파일을 열면서 별개로 중요한 걸 발견했습니다.

**발견한 것**: 실제 `dstone-ai-engine` 코드가 지금 `Workflow`/`WorkFlow` 표기가 **섞여** 있습니다.
- `WorkFlowController`, `WorkFlowDefinition`, `WorkFlowContext`, `WorkFlowExecutor` → 이미 대문자 F로 되어있음 (커밋 `5aa9160 Fix: Update file name casing`)
- `WorkflowRegistry`, `WorkflowRequest`, `WorkflowResponse`, `WorkflowStatusResponse`, `WorkflowSubmitResponse` → 아직 소문자 f
- 변수명도 뒤섞여 있음: `workFlowExecutor`(필드, 대문자 F) vs `workflowRegistry`(필드, 소문자 f) 같은 줄에 공존

제가 지난 턴들에서 이걸 "캐주얼한 타이핑"으로 보고 계획서 전체를 `Workflow`(소문자 f)로 통일했는데, 실제 코드에 이미 부분적으로 `WorkFlow`(대문자 F)가 적용돼 있는 걸 지금 봤습니다. 이게 (a) 사용자님이 의도적으로 `WorkFlow`로 가려다 일부만 하신 건지, (b) 대소문자 관련 별개의 기술적 이슈(파일시스템 케이스 민감도 등)로 우연히 이렇게 된 건지 확인이 필요합니다 — 계획서 전체(수백 군데)의 표기를 좌우하는 문제라 짐작으로 밀고 나가지 않겠습니다.

---

## 답변

Locking in `WorkFlow` (capital F) as the standard. Let me now do the full sweep — parallelGroup design notes first, then the systematic rename.

---

## 답변

Confirmed clean — now let me add the parallelGroup design notes as a new §1.7.

---

## 답변

두 가지 모두 반영했습니다.

**1. parallelGroup 확인** — 실제 `WorkFlowExecutor.runParallel()` 코드를 확인했고, 그대로 유지됩니다. 새 §1.7에 정리했습니다: 그룹 판별/동시실행/흐름제어(마지막 step만 onSuccess·onFailure 사용)는 기존 그대로, 새로 추가된 부분은 (a) 영속화 단위가 "스텝 하나"가 아니라 "그룹 전체"라는 점, (b) 그룹 멤버 각각이 `ConfigCallLog` 로그를 개별로 남긴다는 점, (c) 새로 생긴 `StepOutput.data` 병합은 YAML 선언 순서로 결정적이라는 점, (d) `APPROVAL`은 PENDING 상태와 병렬 그룹의 조합이 설계돼 있지 않으므로 `parallelGroup`을 아예 금지(fail-fast)한다는 신규 제약입니다.

**2. WorkFlow 표기 통일** — 실제 코드를 열어보다가 `WorkFlowController`/`WorkFlowDefinition`/`WorkFlowContext`/`WorkFlowExecutor`는 이미 대문자 F로 바뀌어 있는데 `WorkflowRegistry`/DTO들/`dstone-boot`쪽은 소문자로 남아있는 반쪽짜리 상태를 발견했습니다 (`5aa9160 Fix: Update file name casing` 커밋). 말씀하신 대로 `WorkFlow`로 전체 통일하기로 했으니, 계획서 전체에서 `Workflow`(대문자 W, 고유명사/클래스명으로 쓰인 것) → `WorkFlow`로 일괄 변경했습니다. URL 경로(`/api/ai/workflow/...`), YAML 키(`workflow:`), 리소스 디렉토리(`resources/workflows/`), 패키지명(`runtime.workflow`), 로거명(`net.dstone.ai.workflow.audit`), `workflow.js` 파일명, `workflowId` 같은 일반 식별자명은 실제 코드의 관례(`workFlowExecutor` 필드는 대문자 F, `workflowId` 파라미터는 소문자 f)를 그대로 따라 손대지 않았습니다.

---

## 질문

dstone-boot 에 dstone-ai-engine 의 관리자 메뉴가 필요할 것 같아.
이 기회에 dstone-boot 의 AI 메뉴를 간결하게 정리하고 싶은데. 아래와 같이 말이야. 추후 관리자 메뉴가 늘 것 같아.
AI
    테스트    
        ...
    관리자
        ...

---

## 답변

Clean — all consistent now. Summary of the change:

**§11을 "테스트 / 관리자" 두 갈래로 재편**했습니다:

```
AI
├── 테스트 (net.dstone.boot.ai.controller/.service, 기존 그대로)
│   Chat / SQL변환 / WorkFlow 테스트(submit·status만)
└── 관리자 (net.dstone.boot.ai.controller.admin/.service.admin — 신규 하위패키지, /ai/admin/**)
    ├── 문서 임베딩 관리 — DocumentController 이동 (/ai/document → /ai/admin/document)
    └── WorkFlow 실행 관리 — WorkFlowAdminController 신규 (목록/상세/승인, §10 API 기반)
```

핵심 판단: §10에서 새로 생기는 "실행 목록 조회 + 승인/반려"는 개발자가 찔러보는 테스트가 아니라 운영자가 판단·조작하는 관리 기능이라, 지난 턴에 `WorkFlowTestController`에 얹기로 했던 걸 되돌려서 별도 `WorkFlowAdminController`로 분리했습니다. `DocumentController`도 사실 처음부터 "관리" 기능이었으니 같이 옮겼습니다.

**확장성**: dstone-boot에 이미 있는 URL별 역할 매핑 메커니즘(`ConfigSecurity`, DB의 `PROG_URL`/`ROLE_ID`)을 그대로 활용해 `/ai/admin/**` 전체를 관리자 역할로 묶었습니다 — 말씀하신 대로 관리자 메뉴가 늘어도 이 프리픽스 하나로 계속 커버됩니다.

---

## 질문

OK 이 계획을 진행해줘.

<공통지침>
- 모든 소스의 주석이 반드시 쉽고 직관적이고 명확해야 하며 자세해야 한다.
- 기존 주석에 변경이력설명이 과도하게 들어있지 않도록 작성한다. (과친절로 인한 소스 가독성 저하 방지)

---

## 답변

This is a large multi-phase job — I'll work through it phase by phase, starting with Phase 1 (the foundational package restructure), verify it compiles, then continue. Let me re-read the finalized plan first to work from the exact spec.

---

## 답변

Given the scope, I'll implement this in dependency order rather than strictly by the plan's phase numbers (Phase 1 and 2 are tightly coupled — `StepRunner` needs `WorkFlowExecution` to exist). I'll build the core engine rewrite first (Phases 1+2 content), verify it compiles, then continue with RAG/MCP/dstone-boot in follow-up passes. Let me gather all the source files I need to work from.

---

## 답변

Now I have everything needed. Let me start writing. First, the `runtime.status` package — pure value types with no dependencies, so they come first.

---

## 답변

Now let's remove the old flat files and update `common.definition`:

---

## 답변

I need to fix an oversight — `WorkFlowExecution` needs a `sessionId` field since `AgentExecutor` requires one for chat-memory continuity across AGENT/SUPERVISOR steps. Let me correct that before building on top of it.

---

## 답변

Now the core rewrite — `WorkFlowExecutor`:

---

## 답변

I need to fix a naming collision — a record's auto-generated accessor `pending()` conflicts with my static factory method of the same name.

---

## 답변

Let me centralize that duplicated `"__previous"` magic string into `Constants`, matching the codebase's existing pattern:

---

## 답변

Now update the three usage sites to reference the constant instead of literals:

---

## 답변

Now let's update `ConfigCallLog` — add the dedicated Step IN/OUT advice, exclude it from the generic runtime advice, and fix the `promptName()` reference that no longer exists:

---

## 답변

Now `AgentExecutor` — swap `PromptTemplateRegistry`/`RagService` for inline prompt rendering + `RagRetrievalChain`:

---

## 답변

Now the two usage sites — the prompt-render step (3) and the RAG step (4):

---

## 답변

Now let's split `RagService` into `common.rag.RagRetrievalChain` (search/advisor) and `api.service.EmbedService` (ingest/delete):

---

## 답변

Now `EmbedController` (renamed from `RagController`, `/api/ai/embed`, ingest/delete only — no `search`):

---

## 답변

Now rewrite `WorkFlowController`:

---

## 답변

Now the new `WorkFlowExecutionController` (§10 — list/detail/decision):

---

## 답변

Now the tools rename + `RagSearchTool` + `ExternalProcessTool` base class:

---

## 답변

Now the refactored `ShellExecTool` and `PythonExecTool`:

---

## 답변

Now let's inline the prompts and remove the old prompt infrastructure:

---

## 답변

Now let's try to compile and see what's broken. First ensure `dstone-common` is available locally:

---

## 답변

Good, already installed. Let's compile:

---

## 답변

Excellent — clean compile on the first real attempt. Let me sweep for any stale references I might have missed in configs/docs/comments:

---

## 답변

You've hit your session limit · resets 5:40pm (Asia/Seoul)

---

## 질문

Your claude.ai usage limit has reset. Continue the task you were working on when the limit was reached; do not repeat work that is already complete.

---

## 답변

Now trim the change-history framing in `WorkFlowExecutionService`'s class doc, per the "no excessive change-history explanation" instruction:

---

## 답변

Let me check for stray plan-document section references (`§...`) that shouldn't live in shipped code comments:

---

## 답변

Good, reads fine. Now the last two:

---

## 답변

Let me fix these too:

---

## 답변

Let's also verify a full package build (not just compile) works, since that exercises resource filtering and the Spring Boot repackaging step:

---

## 답변

Phase 1+2 (the core engine rewrite) is done and verified — `mvn clean package` succeeds, jar builds cleanly, and only `dstone-ai-engine` was touched (67 files: 22 new, 24 deleted, 21 modified).

**Implemented and compiling:**
- `runtime` split into `workflow`/`agent`/`step`/`tool`/`status`
- `StepRunner` common interface + `StepInput`/`StepOutput`/`StepResult`, `APPROVAL` step type + `ApprovalStepRunner`, `StepType.RAG` removed
- `WorkFlowExecution`/`StepHistoryEntry`/`WorkFlowExecutionStore`(JdbcTemplate)/`WorkFlowExecutionService` — persisted execution model, `WorkFlowExecutor` rewritten to dispatch uniformly and persist after every step/group (parallelGroup preserved, incl. the APPROVAL-can't-be-parallel guard)
- `ConfigCallLog` extended with the `StepRunner.run(..)` WF_STEP audit-log advice
- `RagService` split into `EmbedService`(ingest/delete, `/api/ai/embed`) + `RagRetrievalChain`(search/advisor, `common.rag`) + new `RagSearchTool`
- Prompts inlined into `agents/*.yml`; `common.prompt` package and `resources/prompts/` deleted
- Tools renamed (`DateTimeTool`/`SqlSyntaxTool`) + `ExternalProcessTool` base class for Shell/Python
- New `WorkFlowExecutionController` (list/detail/decision), `WorkFlowController` rewired to the new service, DB schema file, sample approval workflow for testing
- Swept all comments for stale references and dropped the plan-doc `§N` citations that had leaked into shipped code

**Deferred (not touched yet):**
- Phase 3: RAG retrieval chain still uses the existing `QuestionAnswerAdvisor` (relocated, not yet upgraded to `RetrievalAugmentationAdvisor`)
- Phase 4: MCP client (needs a new Maven dependency + verification)
- Phase 5: `dstone-boot` changes (admin/test menu split, `WorkFlowAdminController`, `DocumentController` URL update) — until this lands, dstone-boot's existing screens will break against the new `/api/ai/embed` and execution-based workflow contract
- `docs/09.dstone-ai-engine.md` hasn't been updated yet to reflect any of this

Given the size, I'd like to pause here rather than plow into Phase 5 (a separate module) unannounced. Want me to continue straight into Phase 5 (dstone-boot) next, or would you rather review this chunk first?

---

## 질문

계속 진행해줘

---

## 답변

Continuing with Phase 5 (dstone-boot). Let me gather the remaining files I need first.

---

## 답변

Good — checked the security seed data (`SAMPLE_ROLE_PROG`). Modifying it safely requires knowing the real deployment's role taxonomy, which I don't have visibility into, so I'll leave that as a documented operational step rather than guess at DB seed changes. Let's build the code now.

---

## 답변

Now remove the old (non-admin) `DocumentController`/`DocumentService`:

---

## 답변

Now the service and controller:

---

## 답변

Now let's rename the dstone-boot Workflow* test classes to `WorkFlow*` (matching the repo-wide capitalization decision) and update the submit/status logic per §11.3:

---

## 답변

Now delete the old lowercase-f VO files, and write the new controller/service:

---

## 답변

Now update `workflow.js`/`workflow.jsp` for the `jobId`→`executionId` rename:

---

## 답변

Now move `document.jsp` to the admin views path with updated URLs:

---

## 답변

Now update `ai/index.jsp` for the two-section (테스트/관리자) layout:

---

## 답변

Just two stale mentions left — quick fix:

---

## 답변

Let's do the same `§`-reference sweep on the new dstone-boot files, and confirm scope stayed contained:

---

## 답변

Both modules build and package cleanly. Phase 5 (dstone-boot) is done:

**dstone-boot changes (31 files):**
- `DocumentController`/`DocumentService` moved to `controller.admin`/`service.admin`, URL `/ai/document/*` → `/ai/admin/document/*`, calls now hit `/api/ai/embed/documents`
- New `WorkFlowAdminController`/`WorkFlowAdminService` + 4 new VOs (`WorkFlowExecutionSummaryResult`/`WorkFlowExecutionDetailResult`/`WorkFlowStepHistoryResult`/`WorkFlowDecisionRequest`) at `/ai/admin/workflow/*` — list/detail/decision screen calling the engine's `/api/ai/workflow/executions` API
- `WorkflowTestController`/`Service`/7 VOs renamed to `WorkFlow*` and updated: `submit.do` response field `jobId`→`executionId`, `status.do` now calls `GET /api/ai/workflow/executions/{executionId}` instead of the old Redis-backed endpoint
- New `workflow-admin.js` + admin `workflow.jsp`; `document.jsp` moved under `ai/admin/document/`; `workflow.js`/`workflow.jsp` updated for the `executionId` rename; `ai/index.jsp` now has 테스트/관리자 sections; 2 small CSS additions for the new badge states
- `SqlConvertService` (unaffected by the plan) only needed its one import fixed for the `WorkFlowCallResult` rename — confirmed no other behavior change needed there, matching what I'd predicted

**Not done:** the `SAMPLE_ROLE_PROG` DB seed row restricting `/ai/admin/**` to an admin role — I didn't want to guess at the real deployment's role taxonomy, so that's a manual step, not code.

Both `mvn clean package` runs succeed (jar + war produced), and only `dstone-ai-engine`/`dstone-boot` were touched — nothing else in the repo.

That's Phases 1, 2, and 5 done. Remaining: **Phase 3** (RAG → `RetrievalAugmentationAdvisor`, currently still on the older `QuestionAnswerAdvisor` which still works), **Phase 4** (MCP client), and the `docs/09.dstone-ai-engine.md` update. Want me to keep going into those, or pause here for you to look over what's landed so far?
