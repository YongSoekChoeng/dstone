# localcommandcaveatCa

> 생성일시: 2026-08-29 21:57

## 질문

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

---

## 질문

<command-name>/clear</command-name>
            <command-message>clear</command-message>
            <command-args></command-args>

---

## 질문

현재 dstone 프레임워크 구조에서 메소드 호출 시 로그가 남게 되어있는데 특정한 메소드(특히 polling 형식)에 대해서만 남지 않게 하려면 어떻게 해야 할까?
net.dstone.boot.common.config.ConfigAspect 에서 하드코딩 해야하나?
그리고 쿼리로그도 같이 적용하고 싶은데... 어떤 방법이 가장 효과적일까?

---

## 답변

현재 구조를 보면 두 로깅이 완전히 다른 메커니즘이라 접근법을 나눠야 합니다.

## 1. 메소드 호출 로그 (ConfigAspect)

`if(methodName.equals("poll..."))` 식으로 Aspect 안에 하드코딩하는 건 피하는 게 좋습니다 — 폴링 메소드가 늘어날 때마다 공통 모듈(`dstone-boot`/`dstone-batchadmin` `ConfigAspect.java`)을 계속 고쳐야 하고, 어떤 메소드가 로그에서 빠지는지 Aspect 코드를 열어보지 않으면 알 수 없습니다.

대신 **커스텀 어노테이션 + 포인트컷 제외**가 이 구조에 가장 자연스럽습니다:

```java
// dstone-common
@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.METHOD)
public @interface NoAspectLog {}
```

```java
@Around("execution(* net.dstone.*..*Service*.*(..)) && !@annotation(net.dstone.common.config.NoAspectLog)")
public Object doServiceProfiling(ProceedingJoinPoint joinPoint) throws Throwable { ... }
```

Dao도 동일하게. 폴링 메소드 쪽에 `@NoAspectLog` 한 줄만 붙이면 되고, Aspect 자체는 한 번만 수정하면 끝입니다. 이 방식이면 앞으로 "이 메소드도 로그 빼주세요" 요청이 와도 Aspect를 다시 안 건드려도 됩니다.

## 2. 쿼리 로그 (log4jdbc)

여기가 까다로운 지점인데, 지금 `conf/log4j2.xml`을 보면 SQL 타이밍 로그는 `jdbc.sqltiming` 로거 하나가 전역으로 INFO를 찍고 있습니다. log4jdbc는 로거 단위 on/off만 지원하고 **SQL문/호출 메소드 단위 필터링 기능이 없어서**, 애노테이션만으로는 쿼리 로그를 못 끕니다.

가장 효과적인 방법은 위 애노테이션과 **MDC(ThreadContext)를 연결**하는 것입니다:

1. `ConfigAspect`의 Dao Around 어드바이스에서, 대상 메소드에 `@NoAspectLog`가 붙어 있으면 `proceed()` 전후로 `ThreadContext.put("SUPPRESS_SQL_LOG", "Y")` / `ThreadContext.remove(...)` (try-finally)
2. `log4j2.xml`의 `jdbc.sqltiming` Logger에 `ThreadContextMapFilter`를 붙여서 그 키가 "Y"면 DENY:

```xml
<Logger name="jdbc.sqltiming" level="INFO" additivity="false">
    <ThreadContextMapFilter onMatch="DENY" onMismatch="NEUTRAL">
        <KeyValuePair key="SUPPRESS_SQL_LOG" value="Y"/>
    </ThreadContextMapFilter>
    <AppenderRef ref="console"/>
    <AppenderRef ref="rollingFile"/>
</Logger>
```

이렇게 하면 커스텀 Filter 클래스도 필요 없고(log4j2 내장 기능), Dao 메소드 하나에 애노테이션 하나 붙이는 걸로 "메소드 호출 로그 + 그 안에서 나가는 쿼리 로그"가 동시에 꺼집니다 — 스위치가 하나라 관리 포인트도 하나입니다.

트레이드오프: MDC는 쓰레드 경계를 못 넘으므로, 폴링 메소드가 별도 쓰레드/비동기로 쿼리를 던지면(`@Async`, 별도 ExecutorService 등) 이 방식이 안 먹습니다. 그런 케이스가 있는지 알려주시면 그 부분도 반영해서 설계할게요.

원하시면 `dstone-common`에 어노테이션 추가하고 `dstone-boot`/`dstone-batchadmin`의 `ConfigAspect` 및 각 `log4j2.xml`에 반영하는 작업을 진행하겠습니다.

---

## 질문

지금 다른 곳은 모두 수정했어. 다만 log4j2.xml 에도 위의 지침대로 했음에도 불구하고 로그가 노출되는데 소스를 보고 수정해줘

---

## 답변

두 가지 문제를 찾았습니다.

## 원인 1: `dstone-boot/conf/log4j2.xml`에 필터가 빠져 있음

필터를 `dstone-common/conf/log4j2.xml`에만 추가하셨는데, `dstone-common`은 라이브러리(JAR)라 실행 시 자체 `log4j2.xml`을 쓰지 않습니다. 실제 폴링 코드(`OutboxDao`)가 도는 곳은 `dstone-boot`이고, `dstone-boot`는 `src/main/resources`에 자체 `log4j2.xml`이 없이 오직 `conf/log4j2.xml`만 사용합니다(pom.xml에서 서버 배포 시 `log4j2.xml`을 War 리소스에서 제외하도록 되어 있음 — `conf/` 버전이 항상 유효 설정). 즉 `SUPPRESS_SQL_LOG`를 아무리 세팅해도 걸러줄 필터 자체가 없었던 것입니다.

## 원인 2: `ConfigAspect.doDaoProfiling`의 어노테이션 체크가 항상 실패(또는 항상 성공)

```java
if( joinPoint.getClass().getAnnotationsByType(net.dstone.common.annotation.NoAspectLog.class) != null) {
```

`joinPoint.getClass()`는 `@NoAspectLog`가 붙은 대상 메소드가 아니라 AspectJ 내부 구현 클래스(`JoinPointImpl` 등)를 가리킵니다. 게다가 `getAnnotationsByType`은 못 찾아도 `null`이 아니라 빈 배열을 반환하므로 이 조건은 **항상 true** — 결과적으로 모든 Dao 호출에서 무조건 `SUPPRESS_SQL_LOG`만 세팅되고 `[DAO]` 로그 자체는 전부 사라지는 상태였습니다(쿼리 로그는 필터가 없어 그대로 노출). Controller/Service 어드바이스처럼 실제 메소드의 `Method` 객체에서 애노테이션을 읽어야 합니다.

두 곳 모두 고치겠습니다.

---

## 답변

Good, my import edit applied. Now fixing the actual detection logic:

---

## 답변

Now add the missing filter to the config that actually governs the running `dstone-boot` app:

---

## 답변

dstone-batch has no Outbox implementation or Dao logging aspect at all, so it's out of scope here. Let's verify the boot module compiles cleanly with these changes.

---

## 답변

Maven isn't available in this environment, so I can't compile-check, but let me review the final file by eye.

---

## 답변

Looks correct. Summary of the fix:

**원인 1 — `dstone-boot/conf/log4j2.xml`에 필터 누락:** 필터를 `dstone-common/conf/log4j2.xml`에만 추가하셨는데, `dstone-common`은 JAR 라이브러리라 자체 로그 설정을 쓰지 않습니다. `OutboxDao`가 실제로 도는 `dstone-boot`는 `src/main/resources`에 자체 `log4j2.xml`이 없고 `conf/log4j2.xml`만 유효 설정으로 쓰이는데, 그 파일엔 `ThreadContextMapFilter`가 없었습니다 → `jdbc.sqltiming`에 추가했습니다.

**원인 2 — `ConfigAspect.doDaoProfiling`의 애노테이션 판별 로직 버그:** `joinPoint.getClass().getAnnotationsByType(...)`는 대상 메소드가 아니라 AspectJ 내부 조인포인트 구현 클래스를 검사하는 코드였고, `getAnnotationsByType`은 못 찾아도 빈 배열(≠ null)을 반환하므로 조건이 항상 `true`였습니다 — 즉 모든 Dao 호출에서 `[DAO]` 애플리케이션 로그 자체가 무조건 사라지고 있었습니다(반대로 원인 1 때문에 SQL 로그는 계속 노출). `MethodSignature`에서 실제 메소드를 꺼내 `@NoAspectLog` 유무를 정확히 판별하도록 고쳤습니다.

두 수정 모두 `dstone-boot`에만 적용했습니다 (`dstone-batch`/`dstone-batchadmin`에는 `OutboxDao`/`@NoAspectLog` 사용처가 없음).
