# localcommandcaveatCa

> 생성일시: 2026-09-23 08:46

## 질문

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

---

## 질문

<command-name>/clear</command-name>
            <command-message>clear</command-message>
            <command-args></command-args>

---

## 질문

<pasted_content id="f5ee">
지금 dstone-ai-engine 에 대한 프로그램구조를 바꾸고 싶어. 
기존 소스구조를 대폭적으로 바꾸어도 상관 없으니까 요청대로 진행해줘.
다만 주석들은 네가 바꾸면 바꿀수록 어지러워 지는 것 같아. 나중에 한꺼번에 정리할 거니까 주석은 이해할 수 있는 범위에서 최소한만 수정해줘.
아래의 내용을 진행하기 위한 계획을 세워줘. 

그리고 그 내용은 아래에도 기록해줘.
/app/dstone/docs/temp/dstone-ai-engine-refactory-20260923.md

<요청내용>
지금은 타입케이스들이 조금 방만해서 조금 정리를 해서 한눈에 파악이 가능하도록 리팩토링 중이야.
뭔가 분류에 통일성이 있어야 할 것 같고 IN/OUT 도 통일성이 필요해.

1. 패키지 구성 및 클래스명 재정의.
일차적으로 패키지 구성 및 클래스명명이 맘에 안들어.
1레벨의 패키지 api/common/runtime/tools 는 상관없어. 그런데 그 밑으로 들어가면 뭔가 섞여있는 느낌이야.
이를테면 net.dstone.ai.common.definition 안에는 설정파일에 맵핑되는 클래스들이 들어가야 하는데 
McpTransport, StepType 등이 있어. 이런 enum 류는 net.dstone.ai.common.consts 안으로 들어가는게 맞지 않나?
그리고 net.dstone.ai.runtime.status 내에는 뭔가 잡다구리한 클래스들이 모여있는 느낌이야.

2. StepType 분류 재정의.
현재 아래와 같이 StepType 이 있는데 
AGENT / SUPERVISOR / ROUTER / TOOL / APPROVAL
이중 AGENT / SUPERVISOR / ROUTER 는 LLM 을 호출하고 나머지 TOOL / APPROVAL 는 LLM 을 호출하지 않는 걸로 보여.
성격이 다른 만큼 재정리가 필요하지 않을까?

3. IN/OUT 재정의.
runtime 내의 Executor/Runner 들의 IN/OUT 을 정리하고 싶어. 항목이 너무 많아지고 필요없이 중첩되는 것도 많아지는 것 같아.
꼭 필요한 내용들로만 정리하고 중첩되는 항목들은 삭제 및 정리를 해줘.
</pasted_content id="f5ee">

---

## 답변

계획을 세워서 `/app/dstone/docs/temp/dstone-ai-engine-refactory-20260923.md`에 기록했습니다. 핵심 내용 요약드립니다.

**조사하면서 확인한 사실**
- `runtime.workflow.WorkFlowContext`는 아무도 참조하지 않는 죽은 클래스
- `StepFlow.error()`/`waitingApproval()`, `StepStatus.ERROR`/`WAITING_APPROVAL`은 정의만 있고 실제로 한 번도 만들어지지 않는 죽은 분기 (grep으로 확인)
- `StepPayload`는 `StepOutput`의 성공 케이스 필드(`primaryText`, `data`)와 100% 동일한 중복 타입
- YAML 14개 파일이 `type: AGENT/TOOL/...` 문자열을 그대로 쓰고 있어, **StepType enum 이름 자체는 바꾸지 않는 쪽**으로 잡았습니다

**세 가지 요청에 대한 방향**
1. **패키지 정리**: `StepType`/`McpTransport`를 `common.definition` → `common.consts`로 이동. `runtime.status`(10개 클래스 뒤섞임)는 패키지 자체를 폐지하고, 각 클래스를 실제 주인(`runtime.agent`/`runtime.tool`/`runtime.step`/`runtime.workflow.execution`) 옆으로 재배치.
2. **StepType 분류**: Runner 구조(AgentStepRunner/ToolStepRunner/ApprovalStepRunner)가 이미 LLM 호출 여부로 나뉘어 있으므로, YAML 값은 그대로 두고 `StepType`에 `Kind.AGENT_CALL / DETERMINISTIC` 분류만 추가해서 코드가 그 구분을 선언적으로 답할 수 있게 함.
3. **IN/OUT 정리**: `StepOutput+StepResult+StepPayload`를 Java 21 sealed interface `StepOutcome`(Success/Routed/Failure/Pending)으로, `StepFlow+StepStatus`를 `WorkflowTransition`(NextStep/Loop/Done/Failed)으로 통합해 죽은 값과 중복 타입을 제거.

주석은 이동/재구성된 부분만 최소 수정하고, 새로 생기는 타입에만 짧은 설명을 붙이는 원칙으로 잡았습니다. 문서 끝(§9)에 진행 전 확인이 필요한 결정 사항 5가지를 남겨뒀습니다 — sealed interface 도입 여부, WorkFlowContext 삭제 여부, ToolOutput 개명 여부, Kind 분류 활용 범위, 진행 방식(한 번에 vs 단계별)입니다.

계획대로 실제 코드 리팩토링을 지금 시작할까요, 아니면 §9 먼저 확인해주시겠어요?
