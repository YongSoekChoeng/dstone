# 오늘_날짜가_어떻게_되지

> 생성일시: 2026-08-28 16:15

## 질문

오늘 날짜가 어떻게 되지?

---

## 답변

오늘 날짜는 2026년 8월 28일입니다.

---

## 질문

dstone-boot에 사가+아웃박스패턴을 사용하는 샘플을 만들어야 할 것 같아.

---

## 답변

핵심 인프라(SagaOrchestrator, OutboxAppender/Relay, DAO/XML)는 이미 배선돼 있어서, 이번엔 도메인 스텝 3개 + 컨트롤러 + Kafka 리스너만 새로 만들면 됩니다. 그런데 코드를 보니 짚고 넘어가야 할 게 하나 있습니다.

`SagaOrchestrator.compensate()`가 **실패한 스텝 자신의 compensate()만** 호출하고, 그 이전에 이미 성공한 스텝들은 되돌리지 않습니다. 즉 지금 엔진 그대로면 "결제 실패 → 재고차감 롤백" 같은 진짜 사가 보상 흐름은 안 나오고, 실패한 결제 스텝 자기 자신의 compensate()만 (의미 없이) 호출됩니다.

---

## 답변

Now the orchestrator itself — cascading compensation, `complete()`, and injecting `SAGA_ID` into step results:

---

## 답변

Now the DAO + XML mapper in dstone-boot:

---

## 답변

Now let's verify everything compiles.

---

## 답변

Good. That all looks correct. Now let's check whether any other `SagaStore` implementations exist (e.g. in dstone-batch) that would break from the new interface method.

---

## 답변

Only one implementation — already updated. Let's double check `bindSingleValue`/`isAjax`/`BaseController` signatures used, and confirm `SagaOrchestrator` bean wiring in `ConfigMessaging` doesn't need changes (it doesn't — constructor signature unchanged).

---

## 답변

Good, `isAjax` matches. Now let's check the `jsonView` view resolver expects `returnObj` in the model (matches other samples) and confirm `RequestUtil` constructor signature used elsewhere matches `new RequestUtil(request, response)`.

---

## 답변

완성했습니다. Maven이 이 환경에 없어서 실제 빌드는 못 돌려봤고, 코드는 꼼꼼히 리뷰했습니다 — DB에 붙여서 최종 확인은 필요합니다.

**엔진 수정 (dstone-common)**
- `SagaOrchestrator.compensate()`: 실패한 스텝 자신이 아니라, 이 사가에서 이미 성공한 스텝들을 `TB_SAGA_STEP_HISTORY`에서 역순 조회해 각 스텝의 원래 입력값(PAYLOAD)으로 되돌려가며 `compensate()` 호출 — 진짜 캐스케이딩 보상.
- `SagaStore.findSuccessStepHistory()` 추가, `insertStepHistory`에 `PAYLOAD`(JSON) 컬럼 추가.
- `SagaOrchestrator.complete(sagaId, lastStep)` 신규 — 마지막 스텝까지 끝난 걸 아는 호출자(리스너)가 사가를 COMPLETED로 종결.
- 각 스텝 결과 이벤트(`{step}-reply`)에 `SAGA_ID`를 항상 심어서, 리스너가 payload만으로 다음 스텝을 트리거할 수 있게 함.
- `TB_SAGA_STEP_HISTORY`에 `PAYLOAD TEXT` 컬럼 추가 (dstone-common/dstone-boot 스키마 파일 둘 다), `SagaDao`/`SagaDao.xml`에 구현 반영.

**샘플 (dstone-boot, `net.dstone.boot.sample.saga`)**
- `OrderSagaController` — `POST /sample/saga/order/start.do`: 재고차감→결제→주문확정 순으로 사가 시작. `qty>=100`이면 재고부족, `amount>=1000000`이면 결제실패로 데모.
- `InventoryReserveStepHandler` / `PaymentStepHandler` / `OrderConfirmStepHandler` — `SagaStepHandler` 구현체(각각 `@Component`라 `ConfigMessaging`의 `List<SagaStepHandler>`에 자동 수집됨).
- `OrderSagaReplyListener` — `inventoryReserve-reply`/`payment-reply`/`orderConfirm-reply` 토픽을 `@KafkaListener`로 소비해서 다음 스텝을 `proceed()`, 마지막은 `complete()` 호출.

**테스트하려면**: `conf/application.yml`에서 `spring.kafka.enabled=true` + 로컬 Kafka 브로커 필요, DB에는 수정된 `01-create-table-mysql-dstone-messaging.sql`(또는 dstone-boot 쪽 파일)을 실행해 `PAYLOAD` 컬럼이 반영된 `TB_SAGA_STEP_HISTORY`가 있어야 합니다. `amount=2000000`으로 호출하면 결제 실패→재고차감 보상 로그가 찍히는 걸 확인할 수 있습니다.

---

## 질문

지금 saga 패턴 관련 내용을 적용했고 maven 빌드 후 kafka 테스트를 진행했는데 
2026-08-28 03:48:31  INFO [org.apache.kafka.clients.producer.internals.TransactionManager] [Producer clientId=producer-1] ProducerId set to 1000 with epoch 0

이런 로그 후에 테이블조회하는 쿼리가 계속 반복되.

---

## 답변

그 이후에 반복되는 쿼리가 정확히 어떤 것인지에 따라 정상 동작인지 문제인지가 갈립니다. 가장 유력한 후보는 `OutboxRelayScheduler`인데, 이건 설계상 `messaging.outbox.relay-interval-ms`(기본 1000ms)마다 `TB_OUTBOX_MESSAGE`를 `SELECT ... WHERE STATUS='PENDING' ...`로 폴링하도록 되어 있어서 — 정상이라면 이렇게 1초마다 반복 조회되는 게 맞습니다.

몇 가지만 확인해 주시겠어요?

---

## 답변

확인했습니다 — `TB_OUTBOX_MESSAGE` SELECT가 반복되는 건 버그가 아니라 `OutboxRelayScheduler`(`dstone-boot/.../messaging/outbox/OutboxRelayScheduler.java`)가 `@Scheduled(fixedDelayString = "${messaging.outbox.relay-interval-ms:1000}")`로 **1초마다** `findPending()`을 폴링하도록 설계되어 있어서 그렇습니다. 아웃박스 패턴 자체가 원래 이렇게 주기적으로 PENDING 건을 훑는 방식이라 정상 동작입니다.

로그가 시끄럽게 보이는 진짜 이유는 `conf/log4j2.xml`에 있습니다. `jdbc.sqltiming`, `jdbc.resultsettable` 로거가 `INFO`로 켜져 있어서 (dstone-boot 프로젝트 전체 기본 설정, 제가 만든 게 아니라 원래 있던 설정) 실행되는 모든 SQL과 그 결과셋을 콘솔/파일에 그대로 찍습니다. 1초마다 도는 폴링 쿼리라 특히 눈에 띄는 겁니다.

조용하게 만드는 방법은 독립적으로 두 가지입니다:

1. **폴링 주기를 늘린다** — `conf/application.yml`에 `messaging.outbox.relay-interval-ms: 5000` 같은 값 추가 (아웃박스 전용, 다른 SQL 로그엔 영향 없음)
2. **SQL 로그 자체를 끈다** — `jdbc.sqltiming`/`jdbc.resultsettable`을 `OFF`로 (단, 이건 아웃박스뿐 아니라 앱 전체 SQL 로그가 다 꺼짐 — 디버깅용으로 일부러 켜둔 걸 수도 있어서 확인 필요)

---

## 답변

알겠습니다, 지금은 그대로 두겠습니다. 정리하면 — 반복 SELECT는 아웃박스 릴레이 스케줄러의 정상적인 폴링 동작이며, 문제가 아닙니다. 나중에 로그가 계속 거슬리면 `messaging.outbox.relay-interval-ms` 조정이나 log4j2.xml의 `jdbc.sqltiming`/`resultsettable` 레벨 조정 둘 중 하나 말씀해 주시면 바로 적용하겠습니다.

---

## 질문

net.dstone.boot.sample.kafka.service.KafkaService.publish("order-events", "1", Map<String, Object>) 호출 후에 발생하는데...
원래 이게 호출되고 난 후에는 net.dstone.boot.sample.kafka.service.KafkaService.consume(Map<String, Object>) 로 곧장 소비되었었는데...
뭐가 영향을 준거지?

---

## 답변

지금 보신 게 원래 있던 `/kafka/send.do` 데모(`KafkaController → KafkaService.publish("order-events",...)`)이고, 이건 제가 이번에 만든 사가 리스너(`OrderSagaReplyListener`, `order-saga-group`, 완전히 다른 토픽 3개)와는 코드상 겹치는 부분이 없어서, 곧바로 "이것 때문이다"라고 단정하긴 어렵습니다. 다만 짚이는 경로가 몇 개 있어서 정확히 확인하려면 로그가 좀 더 필요합니다.

---

## 답변

좋습니다, 그럼 진짜 문제네요. 추측으로 원인을 좁히기보단 로그를 직접 봐야 할 것 같습니다.

앱 시작 로그에서 Kafka 리스너 컨테이너가 뜨는 부분을 확인해 주시겠어요? 특히 아래를 찾아서 붙여주세요:

1. `inventory-service-group` (order-events 컨슈머)와 `order-saga-group` (새로 추가한 사가 리스너) 둘 다 파티션 할당(`partitions assigned` 또는 `ConsumerCoordinator`) 로그가 정상적으로 찍히는지
2. `ERROR`, `WARN` 레벨로 `UNKNOWN_TOPIC_OR_PARTITION`, `missing topic`, `Failed to start`, `order-saga-group`, `inventoryReserve-reply` 같은 키워드가 있는지
3. Kafka 브로커 쪽에 `inventoryReserve-reply` / `payment-reply` / `orderConfirm-reply` 토픽이 실제로 자동생성됐는지 (`auto.create.topics.enable` 설정에 따라 안 생겼을 수도 있음 — 브로커가 로컬이면 `kafka-topics --list`로 확인 가능)

제가 의심하는 건 새로 추가한 `OrderSagaReplyListener`가 존재하지 않는 토픽(`inventoryReserve-reply` 등)을 구독하면서 리스너 컨테이너 시작 단계에서 오류가 나고, 그 여파로 같은 `kafkaListenerContainerFactory`를 공유하는 다른 리스너(`KafkaService.consume()`)까지 영향을 받았을 가능성입니다. 위 로그를 보면 바로 확인될 것 같습니다.

---

## 질문

2026-08-28 03:58:57  INFO [org.apache.kafka.clients.Metadata] [Consumer clientId=consumer-inventory-service-group-3, groupId=inventory-service-group] Cluster ID: hXt_iW8VQ-mPqDcS1Qz-VQ
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.Metadata] [Consumer clientId=consumer-order-saga-group-4, groupId=order-saga-group] Cluster ID: hXt_iW8VQ-mPqDcS1Qz-VQ
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.Metadata] [Consumer clientId=consumer-order-saga-group-1, groupId=order-saga-group] Cluster ID: hXt_iW8VQ-mPqDcS1Qz-VQ
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.Metadata] [Consumer clientId=consumer-order-saga-group-2, groupId=order-saga-group] Cluster ID: hXt_iW8VQ-mPqDcS1Qz-VQ
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-inventory-service-group-3, groupId=inventory-service-group] Discovered group coordinator 127.0.0.1:9092 (id: 2147483646 rack: null)
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-order-saga-group-1, groupId=order-saga-group] Discovered group coordinator 127.0.0.1:9092 (id: 2147483646 rack: null)
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-order-saga-group-4, groupId=order-saga-group] Discovered group coordinator 127.0.0.1:9092 (id: 2147483646 rack: null)
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-order-saga-group-2, groupId=order-saga-group] Discovered group coordinator 127.0.0.1:9092 (id: 2147483646 rack: null)
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-inventory-service-group-3, groupId=inventory-service-group] (Re-)joining group
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-order-saga-group-4, groupId=order-saga-group] (Re-)joining group
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-order-saga-group-2, groupId=order-saga-group] (Re-)joining group
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-order-saga-group-1, groupId=order-saga-group] (Re-)joining group
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-order-saga-group-4, groupId=order-saga-group] Request joining group due to: need to re-join with the given member-id: consumer-order-saga-group-4-5d8f1fd2-75f1-4eb0-b44d-c66a0975d370
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-inventory-service-group-3, groupId=inventory-service-group] Request joining group due to: need to re-join with the given member-id: consumer-inventory-service-group-3-a669b63a-3a50-4ade-a76c-893c3cfce03a
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-order-saga-group-2, groupId=order-saga-group] Request joining group due to: need to re-join with the given member-id: consumer-order-saga-group-2-448162f5-57b9-41f7-a5e6-cf2b7937bd16
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-order-saga-group-1, groupId=order-saga-group] Request joining group due to: need to re-join with the given member-id: consumer-order-saga-group-1-0f89465c-1326-4141-a28d-54e51e464962
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-order-saga-group-4, groupId=order-saga-group] (Re-)joining group
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-inventory-service-group-3, groupId=inventory-service-group] (Re-)joining group
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-order-saga-group-2, groupId=order-saga-group] (Re-)joining group
2026-08-28 03:58:57  INFO [org.apache.kafka.clients.consumer.internals.ConsumerCoordinator] [Consumer clientId=consumer-order-saga-group-1, groupId=order-saga-group] (Re-)joining group
2026-08-28 03:58:58  INFO [net.dstone.boot.common.config.ConfigAspect] +----->[DAO   ] {OutboxDao.findPending(100)}
2026-08-28 03:58:58  INFO [jdbc.sqltiming] SELECT
            ID, TOPIC, MSG_KEY, PAYLOAD, STATUS, RETRY_CNT
        FROM
            TB_OUTBOX_MESSAGE
        WHERE 1=1
            AND STATUS = 'PENDING'
        ORDER BY
            ID ASC
        LIMIT 100
 {executed in 1 msec}
2026-08-28 03:58:58  INFO [jdbc.resultsettable]

---

## 답변

로그를 보니 원인이 명확합니다. `order-saga-group`이라는 **같은 groupId**로 `order-saga-group-1`, `-2`, `-4` 세 개의 컨슈머 인스턴스가 동시에 붙었는데, 이 세 개가 각각 **서로 다른 토픽**(`inventoryReserve-reply`, `payment-reply`, `orderConfirm-reply`)을 구독하고 있습니다.

Kafka 컨슈머 그룹은 "같은 groupId = 같은 작업을 나눠서 하는 동일 로직의 인스턴스들"이라는 전제로 동작합니다. 그룹 멤버마다 구독 토픽이 다르면, 멤버 하나가 조인할 때마다 그룹 전체가 리밸런스되고(로그에 보이는 반복적인 `(Re-)joining group`), 이 리밸런스가 브로커/네트워크를 계속 흔들면서 `inventory-service-group`(order-events 컨슈머)까지 안정적으로 `partitions assigned` 상태에 못 들어가고 계속 대기하게 되는 겁니다. 3개 리스너 메서드에 groupId를 하나로 통일해서 만든 제 실수입니다.

**고침**: 서로 다른 일을 하는 리스너는 groupId도 달라야 합니다(같은 groupId를 쓰는 건 "같은 토픽을 나눠 처리하는 복제본"에만 해당). 각 스텝-리스너에 고유 groupId를 부여하겠습니다.

---

## 답변

Content unchanged, just relocated. Applying the fix here:

---

## 답변

Good, purely a mechanical rename/repackage consistent with the rest of the codebase — `@Component`→`@Service`, `BaseObject`→`BaseService`, still `implements SagaStepHandler`, still `@Service`-scanned so wiring into `SagaOrchestrator`'s `List<SagaStepHandler>` is unaffected.

That confirms the groupId fix I just applied is the right (and now correctly located) change. Restart the app and re-test — `inventory-service-group` should reach `partitions assigned` and `consume()` should fire normally again.
