# 바로_앞전에_있었던_대화내용이_뭐였지

> 생성일시: 2026-09-24 17:43

## 질문

바로 앞전에 있었던 대화내용이 뭐였지 ?

---

## 답변

바로 앞 세션은 오늘 09:30~09:32에 있었고, 세션이 끊겼다가 다시 접속한 뒤 확인하는 대화였습니다. `History/20260924/0932-...md`에 기록된 내용입니다.

**1. "재접속했는데 반영 안 된 내용이 있는지 확인해줘"**
- 유실된 작업은 없다고 답했습니다. 그 전 세션은 09:22에 정상적으로 끝났고, 결과는 `c73c3fe`(57개 파일)로 커밋과 푸시까지 되어 있었습니다.
- 코드, SQL, JSP, JS 어디에도 옛 이름 `VARIABLES_JSON`이 남아 있지 않았습니다.
- `dstone-ai-engine`과 `dstone-boot`는 다시 컴파일했고 오류가 없었습니다.
- 아직 하지 않은 후속 과제로 네 가지를 안내했습니다.
  1. 로컬 외 환경(k8s 등)의 PostgreSQL에서 `VARIABLES_JSON` 컬럼 이름을 `CONTEXT_JSON`으로 바꾸기
  2. TOOL `parse: json` 실제 실행 검증 (JSON을 돌려주는 샘플 Tool이 아직 없음)
  3. dstone-boot 관리자 Workflow 화면을 직접 열어 확인하기
  4. 8081 포트에서 실행 중이던 엔진 정리

**2. "8081 서버 내려줘"**
- `dstone-ai-engine/bin/stopApp.sh`로 PID 40142를 종료했고, 8081 포트가 비어 있는 것까지 확인했습니다.

그 전 09:22까지의 세션에서는 step 간 데이터 흐름을 재설계하는 작업을 했습니다. 실행 컨텍스트 트리를 도입하고 `VARIABLES_JSON`을 `CONTEXT_JSON`으로 바꿨습니다. 위 후속 과제 1~3번은 아직 남아 있으니, 이어서 진행할 게 있으면 말씀해 주세요.

---

## 질문

이번 변경건에 맞추어서 yml도 모두 바꾼거야 ?

---

## 답변

YAML 쪽은 확인이 거의 끝났고, 문서에 옛 문법이 남았는지만 마저 확인하겠습니다.

---

## 답변

네, 이번 변경에 맞춰 YAML도 모두 바꿨습니다.

**파일 현황**
- 변경 커밋 `c73c3fe`에서 옛 문법을 쓰던 YAML 16개가 수정됐습니다. Workflow 9개, Agent 7개입니다.
- 나머지 12개는 원래 옛 문법을 쓰지 않던 파일이라 고칠 필요가 없었습니다. Agent 6개와 `sample-router-multiway`, `sample-agent-tool-calling`, `sample-approval-pause-resume` 등 Workflow 6개입니다.
  - 이 파일들의 AGENT step에는 `input`이 없습니다. 이 경우 `{{previous.text}}`가 기본값으로 쓰여서 문제없습니다 (`WorkFlowExecutor.java:354`).

**바뀐 키**

| 옛 키 | 새 키 | 수정 건수 |
|---|---|---|
| `inputTemplate` (`{previous}` 문법) | `input` (`{{ }}` 문법) | 12건 |
| `structuredOutput` | `output` | 3건 |
| `forEachVariable` | `forEach` / `itemVariable` | 1건 |

**남은 옛 문법 확인**
- workflows와 agents 폴더 전체를 검색했는데 `inputTemplate`, `forEachVariable`, `structuredOutput`, `{previous}`, `__previous`가 하나도 없습니다.
- `sample-general-chat.yml`의 `{role}`은 Workflow 템플릿 문법이 아닙니다. 채팅 화면이 채워 넣는 system prompt 값이라서 그대로 둔 게 맞습니다.

**옛 키가 남아 있으면 기동에 실패합니다**
- `YamlDefinitionLoader`는 기본 설정의 `ObjectMapper`를 씁니다. 그래서 record에 없는 키가 있으면 해당 파일 경로를 담은 예외를 던지고 기동에 실패합니다.
- `WorkFlowRegistry`도 기동할 때 모든 `{{ }}` 참조를 검증합니다.
- 직전 세션에서 새 코드로 8081 엔진을 띄웠을 때 정상 기동했습니다. 전체 YAML이 새 문법으로 로드된다는 뜻입니다.

문서 `docs/09.dstone-ai-engine.md`에도 옛 이름(`structuredOutput`, `forEachVariable`)이 나오긴 합니다. 다만 모두 날짜가 붙은 과거 검증 기록이나 "폐지된 필드" 설명 안에 있는 것이라 고칠 대상은 아닙니다.

---

## 질문

<pasted_content id="2511">
workFlow(id=sample-mcp-filesystem-list) 의 테스트를 진행했어. 결론적으로는 성공했는데... 
의문인게 list_directory 를 통해서 읽어낸 파일목록이 '[FILE] notes.txt' 인데..
'notes.txt' 로 수정되서 정확하게 파일을 읽어냈거든.
그럼 '[FILE] notes.txt' => 'notes.txt' 이렇게 수정한 게 정확하게 누구이며 어떤 원리로 그렇게 읽어냈나?

<로그>
||===================================== [net.dstone.ai.api.controller.WorkFlowController] START ======================================||
2026-09-24 05:30:36  INFO [net.dstone.ai.common.config.ConfigCallLog] +->[CONTROLLER] {WorkFlowController.submit(String[sample-mcp-filesystem-list]WorkFlowRequest[message=sample, sessionId=null, variables=null], , )}
2026-09-24 05:30:36  INFO [net.dstone.ai.common.config.ConfigCallLog] +--->[SERVICE ] {WorkFlowExecutionService.submitAsync(WorkFlowDefinition[id=sample-mcp-filesystem-list, description=외부 MCP 서버(filesystem)의 Tool 2개(list_directory→read_text_file)를 TOOL step에서 LLM 없이 직접 호출하는 확인용 Workflow., maxIterations=3, allowedCallers=null, steps=[StepDefinition[id=list, type=TOOL, ref=list_directory, onFailure=FAIL, onSuccess=read-notes, inputTemplate={"path":"{previous}"}, approverRole=null, structuredOutput=true, routes=null, forEachVariable=null, itemVariable=null], StepDefinition[id=read-notes, type=TOOL, ref=read_text_file, onFailure=FAIL, onSuccess=SUCCESS, inputTemplate={"path":"D:/AppHome/framework/dstone/dstone-ai-engine/mcp/server-filesystem/sample/{previous}"}, approverRole=null, structuredOutput=true, routes=null, forEachVariable=null, itemVariable=null]]]String[d5808769-de55-4e0d-966a-65805b2bdcf8], , , String[sample], )}
2026-09-24 05:30:36  INFO [jdbc.sqltiming] INSERT INTO AI_WORKFLOW_EXECUTION (  EXECUTION_ID, WORKFLOW_ID, CALLER, SESSION_ID, STATUS, CURRENT_STEP_INDEX, VARIABLES_JSON, RESULT_TEXT, ERROR_MESSAGE, CREATED_AT, UPDATED_AT ) VALUES (   '0c27dfba-8107-4800-b11d-7db6a6ce329e', 'sample-mcp-filesystem-list', NULL, 'd5808769-de55-4e0d-966a-65805b2bdcf8', 'RUNNING', 0, '{"__previous":"sample"}'::jsonb, NULL, NULL, '09/24/2026 17:30:36.332', '09/24/2026 17:30:36.332' )
 {executed in 145 msec}
||===================================== [net.dstone.ai.api.controller.WorkFlowController] END ======================================||

|--------------------------------------------------------------------------------------------------------------------------------------|
[WorkFlowExecutor - workFlow(id=sample-mcp-filesystem-list)] Start !!!
<input>
WorkFlowDefinition[id=sample-mcp-filesystem-list, description=외부 MCP 서버(filesystem)의 Tool 2개(list_directory→read_text_file)를 TOOL step에서 LLM 없이 직접 호출하는 확인용 Workflow., maxIterations=3, allowedCallers=null, steps=[StepDefinition[id=list, type=TOOL, ref=list_directory, onFailure=FAIL, onSuccess=read-notes, inputTemplate={"path":"{previous}"}, approverRole=null, structuredOutput=true, routes=null, forEachVariable=null, itemVariable=null], StepDefinition[id=read-notes, type=TOOL, ref=read_text_file, onFailure=FAIL, onSuccess=SUCCESS, inputTemplate={"path":"D:/AppHome/framework/dstone/dstone-ai-engine/mcp/server-filesystem/sample/{previous}"}, approverRole=null, structuredOutput=true, routes=null, forEachVariable=null, itemVariable=null]]]WorkFlowExecution[executionId=0c27dfba-8107-4800-b11d-7db6a6ce329e, workflowId=sample-mcp-filesystem-list, caller=null, sessionId=d5808769-de55-4e0d-966a-65805b2bdcf8, status=RUNNING, currentStepIndex=0, variables={__previous=sample}, resultText=null, errorMessage=null, createdAt=2026-09-24T08:30:36.332270700Z, updatedAt=2026-09-24T08:30:36.332270700Z], 
|--------------------------------------------------------------------------------------------------------------------------------------|

|--------------------------------------------------------------------------------------------------------------------------------------|
[StepRunner - step(id=list, type=TOOL, ref=list_directory)] Start !!!
<input>
WorkFlowExecution[executionId=0c27dfba-8107-4800-b11d-7db6a6ce329e, workflowId=sample-mcp-filesystem-list, caller=null, sessionId=d5808769-de55-4e0d-966a-65805b2bdcf8, status=RUNNING, currentStepIndex=0, variables={__previous=sample}, resultText=null, errorMessage=null, createdAt=2026-09-24T08:30:36.332270700Z, updatedAt=2026-09-24T08:30:36.332270700Z]StepDefinition[id=list, type=TOOL, ref=list_directory, onFailure=FAIL, onSuccess=read-notes, inputTemplate={"path":"{previous}"}, approverRole=null, structuredOutput=true, routes=null, forEachVariable=null, itemVariable=null], StepInput[renderedText=sample, variables={__previous=sample}], 
|--------------------------------------------------------------------------------------------------------------------------------------|

|--------------------------------------------------------------------------------------------------------------------------------------|
[Tools - ToolExecutor.call()] Start !!!
<input>
String[list_directory], String[{"path":"sample"}], 
|--------------------------------------------------------------------------------------------------------------------------------------|

|--------------------------------------------------------------------------------------------------------------------------------------|
[Tools - ToolExecutor.call()] End !!!
<output>
[FILE] notes.txt
|--------------------------------------------------------------------------------------------------------------------------------------|

|--------------------------------------------------------------------------------------------------------------------------------------|
[StepRunner - step(id=list, type=TOOL, ref=list_directory)] End !!!
<output>
Success[primaryText=[FILE] notes.txt, data={}]
|--------------------------------------------------------------------------------------------------------------------------------------|

|--------------------------------------------------------------------------------------------------------------------------------------|
[StepRunner - step(id=read-notes, type=TOOL, ref=read_text_file)] Start !!!
<input>
WorkFlowExecution[executionId=0c27dfba-8107-4800-b11d-7db6a6ce329e, workflowId=sample-mcp-filesystem-list, caller=null, sessionId=d5808769-de55-4e0d-966a-65805b2bdcf8, status=RUNNING, currentStepIndex=1, variables={__previous=[FILE] notes.txt}, resultText=null, errorMessage=null, createdAt=2026-09-24T08:30:36.332270700Z, updatedAt=2026-09-24T08:30:36.809085300Z]StepDefinition[id=read-notes, type=TOOL, ref=read_text_file, onFailure=FAIL, onSuccess=SUCCESS, inputTemplate={"path":"D:/AppHome/framework/dstone/dstone-ai-engine/mcp/server-filesystem/sample/{previous}"}, approverRole=null, structuredOutput=true, routes=null, forEachVariable=null, itemVariable=null], StepInput[renderedText=[FILE] notes.txt, variables={__previous=[FILE] notes.txt}], 
|--------------------------------------------------------------------------------------------------------------------------------------|

|--------------------------------------------------------------------------------------------------------------------------------------|
[Tools - ToolExecutor.call()] Start !!!
<input>
String[read_text_file], String[{"path":"D:/AppHome/framework/dstone/dstone-ai-engine/mcp/server-filesystem/sample/notes.txt"}], 
|--------------------------------------------------------------------------------------------------------------------------------------|

|--------------------------------------------------------------------------------------------------------------------------------------|
[Tools - ToolExecutor.call()] End !!!
<output>
dstone-ai-engine MCP 샘플용 테스트 파일입니다.

이 파일은 sample-filesystem-mcp.yml에 정의된 MCP 필독시스템 서버(공식 @modelcontextprotocol/server-filesystem)가
이 디렉토리(mcp-sandbox/sample) 안에서만 파일을 읽고 목록을 볼 수 있도록 허용해 준 사례 파일입니다.

- 이 서버는 STDIO 트랜스포트로 접속됩니다(ConfigMcp가 기동 시 npx로 이 서버 프로세스를 직접 실행합니다).
- allowedTools는 list_directory / read_text_file 두 개로만 제한해 두었습니다(읽기 전용 데모).
- sample-mcp-filesystem-agent.yml Agent에게 "mcp-sandbox/sample 폴더에 어떤 파일이 있는지 알려줘"라고
  물어보면, 이 Tool을 스스로 호출해서 답합니다.
- sample-mcp-filesystem-list.yml Workflow는 LLM 없이 TOOL step에서 list_directory를 직접 호출합니다.
|--------------------------------------------------------------------------------------------------------------------------------------|

|--------------------------------------------------------------------------------------------------------------------------------------|
[StepRunner - step(id=read-notes, type=TOOL, ref=read_text_file)] End !!!
<output>
Success[primaryText=dstone-ai-engine MCP 샘플용 테스트 파일입니다.

이 파일은 sample-filesystem-mcp.yml에 정의된 MCP 필독시스템 서버(공식 @modelcontextprotocol/server-filesystem)가
이 디렉토리(mcp-sandbox/sample) 안에서만 파일을 읽고 목록을 볼 수 있도록 허용해 준 사례 파일입니다.

- 이 서버는 STDIO 트랜스포트로 접속됩니다(ConfigMcp가 기동 시 npx로 이 서버 프로세스를 직접 실행합니다).
- allowedTools는 list_directory / read_text_file 두 개로만 제한해 두었습니다(읽기 전용 데모).
- sample-mcp-filesystem-agent.yml Agent에게 "mcp-sandbox/sample 폴더에 어떤 파일이 있는지 알려줘"라고
  물어보면, 이 Tool을 스스로 호출해서 답합니다.
- sample-mcp-filesystem-list.yml Workflow는 LLM 없이 TOOL step에서 list_directory를 직접 호출합니다.
, data={}]
|--------------------------------------------------------------------------------------------------------------------------------------|

|--------------------------------------------------------------------------------------------------------------------------------------|
[WorkFlowExecutor - workFlow(id=sample-mcp-filesystem-list)] End !!!
<output>
WorkFlowExecution[executionId=0c27dfba-8107-4800-b11d-7db6a6ce329e, workflowId=sample-mcp-filesystem-list, caller=null, sessionId=d5808769-de55-4e0d-966a-65805b2bdcf8, status=DONE, currentStepIndex=1, variables={__previous=dstone-ai-engine MCP 샘플용 테스트 파일입니다.

이 파일은 sample-filesystem-mcp.yml에 정의된 MCP 필독시스템 서버(공식 @modelcontextprotocol/server-filesystem)가
이 디렉토리(mcp-sandbox/sample) 안에서만 파일을 읽고 목록을 볼 수 있도록 허용해 준 사례 파일입니다.

- 이 서버는 STDIO 트랜스포트로 접속됩니다(ConfigMcp가 기동 시 npx로 이 서버 프로세스를 직접 실행합니다).
- allowedTools는 list_directory / read_text_file 두 개로만 제한해 두었습니다(읽기 전용 데모).
- sample-mcp-filesystem-agent.yml Agent에게 "mcp-sandbox/sample 폴더에 어떤 파일이 있는지 알려줘"라고
  물어보면, 이 Tool을 스스로 호출해서 답합니다.
- sample-mcp-filesystem-list.yml Workflow는 LLM 없이 TOOL step에서 list_directory를 직접 호출합니다.
}, resultText=dstone-ai-engine MCP 샘플용 테스트 파일입니다.

이 파일은 sample-filesystem-mcp.yml에 정의된 MCP 필독시스템 서버(공식 @modelcontextprotocol/server-filesystem)가
이 디렉토리(mcp-sandbox/sample) 안에서만 파일을 읽고 목록을 볼 수 있도록 허용해 준 사례 파일입니다.

- 이 서버는 STDIO 트랜스포트로 접속됩니다(ConfigMcp가 기동 시 npx로 이 서버 프로세스를 직접 실행합니다).
- allowedTools는 list_directory / read_text_file 두 개로만 제한해 두었습니다(읽기 전용 데모).
- sample-mcp-filesystem-agent.yml Agent에게 "mcp-sandbox/sample 폴더에 어떤 파일이 있는지 알려줘"라고
  물어보면, 이 Tool을 스스로 호출해서 답합니다.
- sample-mcp-filesystem-list.yml Workflow는 LLM 없이 TOOL step에서 list_directory를 직접 호출합니다.
, errorMessage=null, createdAt=2026-09-24T08:30:36.332270700Z, updatedAt=2026-09-24T08:30:37.166604300Z]
|--------------------------------------------------------------------------------------------------------------------------------------|
</pasted_content id="2511">

---

## 답변

로그를 보면 이번 리팩터링 전의 옛 코드로 실행된 것 같습니다(`inputTemplate`, `{previous}`, `VARIABLES_JSON`). 그래서 변경 전 코드에서 `[FILE] ` 접두어를 누가 떼어냈는지 찾아보겠습니다.

---

## 답변

`[FILE] ` 접두어를 떼어낸 것은 LLM이나 MCP 서버가 아니었습니다. `dstone-ai-engine`의 `ToolStepRunner` 코드 안에 있던 정규식이 떼어냈습니다. 그것도 원래 다른 용도로 만든 정규식이 우연히 맞아떨어진 것이었습니다.

붙여주신 로그는 리팩터링 전 코드로 실행한 결과입니다. `inputTemplate`, `{previous}`, `VARIABLES_JSON`이 보이는 걸로 알 수 있습니다. 옛 코드 기준으로 보면 이렇게 동작했습니다.

**처리 순서 (옛 `ToolStepRunner.run()`)**
```java
String normalized = this.stripLlmArtifacts(input.renderedText());   // ① 앞 step 출력 정리
String jsonInput  = this.renderToolInput(definition.inputTemplate(), normalized, ...); // ② {previous}에 끼워넣기
```
1. `list` step의 결과는 `[FILE] notes.txt`였고, 이 값이 `__previous`에 저장됐습니다.
2. `read-notes` step이 시작될 때, 이 값을 `{previous}` 자리에 넣기 전에 `stripLlmArtifacts()`가 먼저 실행됐습니다.
3. 이 함수 안에 아래 정규식이 있었습니다.
   ```java
   LEADING_LABEL = Pattern.compile("^\\[[^\\[\\]\\n]{1,60}\\]\\s*\\n*");
   ```
   뜻은 "맨 앞에 있는 `[최대 60자 레이블]`과 뒤따르는 공백을 지운다"입니다. `[FILE] `가 이 모양과 똑같아서 지워졌고 `notes.txt`만 남았습니다.
4. 그 결과가 `.../sample/{previous}`에 들어가 `.../sample/notes.txt`가 됐습니다. 로그의 `ToolExecutor.call()` 입력이 바로 이 값입니다.

**원래는 무엇을 위한 정규식이었나**
- AGENT step의 LLM은 "레이블 붙이지 마"라고 지시해도 `[변환된 SQL]` 같은 머리말이나 코드펜스(```)를 자꾸 붙였습니다.
- 그래서 TOOL step에 넘기기 전에 그런 군더더기를 벗겨내려고 만든 방어 코드였습니다.
- MCP의 `[FILE]` 표기를 처리하려고 만든 게 아니었고, 옛 YAML 주석에도 "정식 기능이 아니라 정규식이 우연히 맞아떨어지는 것"이라고 적혀 있었습니다.
- 파일이 두 개 이상이면 `[FILE] a.txt\n[FILE] b.txt`에서 첫 번째 `[FILE] `만 지워집니다. 그러면 여러 줄이 그대로 경로에 붙어서 실패합니다.

**현재 코드에서는**
이번 리팩터링(`c73c3fe`)에서 이 암묵적인 정리 로직을 없앴습니다. 지금은 `stripLlmArtifacts`나 `LEADING_LABEL`이 코드에 없습니다. 대신 YAML에서 명시적으로 파일명을 뽑습니다.
```yaml
- id: list
  output:
    parse: lines
    pattern: '^\[FILE\] (.+)$'     # [FILE] 줄만 남기고, 괄호 그룹(파일명)만 추출
- id: read-notes
  forEach: steps.list.data.lines   # 파일 개수만큼 병렬 반복
  itemVariable: fileName
  input:
    path: "{{input.message}}/{{fileName}}"
```
이제는 이 YAML 설정이 `[FILE] ` 접두어를 떼어냅니다. 파일이 여러 개여도 파일마다 따로 읽습니다.

한 가지 주의할 점이 있습니다. 지금 테스트한 환경(Windows `D:/...`)은 옛 빌드로 실행 중입니다. 새 코드로 다시 빌드해서 띄우려면 그 환경의 PostgreSQL에서도 컬럼 이름을 먼저 바꿔야 합니다.
```sql
ALTER TABLE AI_WORKFLOW_EXECUTION RENAME COLUMN VARIABLES_JSON TO CONTEXT_JSON;
```
또 새 YAML에서는 message에 `.../mcp/server-filesystem/sample`처럼 디렉토리 절대경로를 넣어야 합니다.
