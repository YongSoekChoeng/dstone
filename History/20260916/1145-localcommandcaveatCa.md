# localcommandcaveatCa

> 생성일시: 2026-09-16 11:45

## 질문

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

---

## 질문

<command-name>/clear</command-name>
            <command-message>clear</command-message>
            <command-args></command-args>

---

## 질문

net.dstone.ai.common.config.ConfigTool 를 보면 
application.yml 의 dstone.ai.tool.allowed-by-caller 에 등록된 내용을 사용하는 것 처럼 되어있는데
실제로 application.yml 에는 해당 프로퍼티가 없지 않은가?

---

## 답변

맞습니다. 확인해보니 실제 동작에는 문제가 없지만, 관찰하신 내용은 정확합니다.

**`dstone-ai-engine/conf/application.yml`**의 168~171번 줄을 보면:

```yaml
# tool:
#     allowed-by-caller:
#         - caller: sample-si-project
#           tools: getCurrentDateTime,validateSqlSyntax
```

`dstone.ai.tool.allowed-by-caller`는 실제 값으로 존재하지 않고, **주석 처리된 예시**로만 남아 있습니다.

**그런데 이게 버그는 아닙니다.** `ConfigTool.allowedToolNames()`가 참조하는 `ConfigProperty.getListProperty()`(`dstone-common`)는 키가 없을 때 예외 없이 빈 `ArrayList`를 반환하도록 되어 있습니다 (`Binder.get(env).bind(...).orElseGet(ArrayList::new)`).

그래서 흐름은:
1. `allowedToolNames(caller)` → `getListProperty("dstone.ai.tool.allowed-by-caller")` → 빈 리스트
2. 빈 리스트를 순회하지만 매칭되는 `caller` 항목이 없음
3. 루프 끝까지 못 찾고 `null` 반환 → `toolCallbackProvider(caller)`에서 "화이트리스트 없음 = 전체 허용"으로 처리

즉, `ConfigTool.java` 34번째 줄 주석에 적힌 "설정에 없는 caller는 화이트리스트가 없는 것으로 보고 등록된 Tool 전체를 허용한다"는 설계가 의도한 그대로 동작하는 상태입니다. `application.yml`의 주석은 이 policy를 실제로 켜고 싶은 SI 프로젝트가 참고할 수 있도록 형식만 남겨둔 것으로 보입니다(`ApiKeyAuthFilter`/`RateLimitFilter`의 `keys`/`overrides`도 같은 List-of-Map 컨벤션을 쓰는 걸로 봐서 일관된 패턴).

정리하면: 코드가 참조하는 프로퍼티가 실제 설정에는 없는 게 맞고, 이는 "미설정 시 전체 허용"이라는 안전한 기본값으로 설계된 것이라 문제 없습니다. 다만 원하시면 주석을 해제하는 대신 문서(`docs/09.dstone-ai-engine.md`)나 yml 주석에 "기본은 비활성화 상태(예시용)"라는 점을 좀 더 명확히 적어둘 수도 있습니다.

---

## 질문

dstone-ai-engine 에서 Tool 설정 및 사용에 대한 제반 사항을 (어떤 구조인지)분석해줘. 
그리고 net.dstone.ai.common.config.ConfigTool 이 반드시 필요한지 분석해줘.

---

## 답변

# 1. dstone-ai-engine Tool 구조 분석

## 전체 흐름

```
[Tool 선언]                    [Tool 등록]                    [Tool 사용]
tools/*/XxxTools.java    →    ConfigTool                →    ① AGENT/SUPERVISOR step (LLM이 스스로 선택)
  @AiTool                     (@PostConstruct에서              AgentExecutor.buildSpec()
  @Tool 메소드                  빈 스캔 → Provider 조립)          → spec.tools(configTool.toolCallbackProvider(caller))
                                                              → Spring AI의 자체 tool-calling 루프

                                                            ② TOOL step (이름으로 직접 호출, LLM 미개입)
                                                              ToolStepRunner → ToolExecutor.call(caller, toolName, json)
                                                              → configTool.findByName(caller, toolName)
```

### ① Tool 선언 (`tools/` 패키지)
`@AiTool`(메타애노테이션 `@Component`)을 클래스에 붙이고 그 안에 Spring AI의 `@Tool` 메소드를 두면 끝. 현재 5종:
- `sample.DateTimeTools` — 등록 방식을 보여주는 샘플
- `sql.SqlSyntaxTools` — JSQLParser로 SQL 문법 검증(자기수정 루프용)
- `shell.ShellExecTool` / `python.PythonExecTool` / `http.HttpCallTool` — 기본 비활성(`dstone.ai.tool.{shell,python,http}.enabled=false`), 화이트리스트된 스크립트/호스트만 실행

### ② 등록 (`ConfigTool`)
`@PostConstruct`에서 `ApplicationContext.getBeansWithAnnotation(AiTool.class)`로 스캔 → `MethodToolCallbackProvider`로 묶어 `ToolCallbackProvider` 하나를 만듦. Spring AI는 `@Tool` 메소드를 자동으로 찾아주지 않으므로(Bean 스캔 후 명시적으로 `toolObjects(...)`에 넘겨야 함) 이 조립 자체가 필요한 배선.

### ③ caller별 화이트리스트
`dstone.ai.tool.allowed-by-caller`(현재 application.yml엔 예시로만 주석 처리, 미설정 시 전체 허용)를 조회해서 caller가 쓸 수 있는 Tool 이름만 필터링. **이 필터링이 딱 한 곳(`ConfigTool.allowedToolNames`)에만 있고**, 아래 두 소비처가 공통으로 그걸 거쳐간다.

### ④ 소비처 (2가지 경로, 둘 다 `ConfigTool`을 거침)
- **AGENT/SUPERVISOR step** (`AgentExecutor.buildSpec()` 5번) — `agent.toolsEnabled()`(또는 `ChatController`의 요청별 override)가 true면 `configTool.toolCallbackProvider(caller)`를 `ChatClient`에 붙임 → 어떤 Tool을 언제 부를지는 LLM이 Spring AI의 tool-calling 루프 안에서 판단
- **TOOL step** (`ToolStepRunner` → `ToolExecutor`) — Workflow YAML에 `ref`로 지정된 Tool 이름을 `configTool.findByName(caller, toolName)`으로 직접 찾아 LLM 없이 호출(결정적 검증용, 예: `validateSqlSyntax`)

두 경로 모두 `ConfigTool`을 통해서만 caller의 화이트리스트를 통과한 Tool에 접근하며, 그 외 클래스는 `ApplicationContext`나 원시 `ToolCallback` 목록을 직접 다루지 않는다.

---

# 2. `ConfigTool`이 반드시 필요한가

**필요하다.** 세 가지 근거:

1. **Spring AI의 Tool 등록은 자동이 아니다.** `@Tool` 메소드가 붙은 빈이 있어도 Spring AI가 알아서 `ChatClient`에 태워주지 않는다 — `MethodToolCallbackProvider.builder().toolObjects(...)`로 누군가는 명시적으로 조립해야 한다. `ConfigTool.discover()`가 그 배선 역할을 한다.

2. **caller별 화이트리스트는 이 엔진만의 도메인 요구사항이라 Spring AI가 대신해줄 수 없다.** `dstone-ai-engine`은 여러 SI 프로젝트가 공유하는 단일 엔진이라(CLAUDE.md에도 명시된 설계 의도), 한 앱에게만 허용된 Tool을 다른 앱이 못 쓰게 막는 로직이 필요하다. 이건 라이브러리가 아니라 이 프로젝트가 직접 구현해야 하는 부분이고, `ConfigTool`이 그 유일한 구현 지점이다.

3. **두 소비 경로(AGENT의 LLM 주도 호출 / TOOL의 이름 직접 호출)가 화이트리스트 로직을 공유해야 한다.** `ConfigTool`이 없다면 `AgentExecutor`와 `ToolExecutor`가 각자 `ApplicationContext` 스캔 + 화이트리스트 파싱을 중복 구현하거나(로직 불일치 위험), 서로 다른 규칙을 적용할 위험이 생긴다. `ToolExecutor`의 주석("모든 TOOL 호출이 반드시 여길 거치므로... governance가 감사/제한하고 싶어지면 이 클래스 하나만 감싸면 된다")과 동일한 이유로, `ConfigTool`도 "Tool 접근 정책"의 단일 지점 역할을 한다.

## 사소하게 짚을 점 (필요성 판단과는 별개)

`toolCallbackProvider()`(인자 없는 버전, 60번 줄)는 현재 코드베이스 어디서도 호출되지 않는다 — `discover()` 내부 로그도 `toolNames()`를 쓰지 `toolCallbackProvider()`를 쓰지 않는다. caller 없이 "전체 Tool 무조건 다 허용" 용도로 남겨둔 공개 API로 보이는데, 실사용처가 없다면:
- 향후 caller 개념이 없는 내부 호출용으로 의도적으로 열어둔 것인지
- 아니면 단순히 죽은 코드인지

확인이 필요하다. 지금 당장 문제는 아니지만, `docs/09.dstone-ai-engine.md`에 이 메소드의 용도가 언급돼 있는지 한번 대조해볼 만하다.

---

## 질문

그러면 caller별 화이트리스트가 설정되지 않은 지금 같은 경우 모든 tool에 대한 권한이 허용된다는 뜻인가?

---

## 답변

네, 맞습니다. 정확히는 두 단계로 나눠서 봐야 합니다.

**1) 화이트리스트 자체가 없는 지금 상황**

`ConfigTool.allowedToolNames(caller)`가 항상 `null`을 반환합니다.
- `allowed-by-caller` 설정이 없으니 `getListProperty()`는 빈 리스트 → 루프에서 매칭되는 `caller` 항목을 못 찾음 → `null` 리턴
- `caller` 자체가 `null`인 경우(예: `dstone.ai.security.auth.enabled=false`라 인증 없이 caller가 항상 null)도 바로 `null` 리턴

그리고 `toolCallbackProvider(caller)`에서 `allowedToolNames == null`이면 필터링 없이 `this.toolCallbackProvider`(등록된 Tool 전체)를 그대로 돌려줍니다. 즉 **caller를 구분하지 않고 등록된 모든 Tool에 접근 가능**합니다.

**2) 단, "모든 Tool"의 범위에는 두 가지 조건이 걸려 있습니다**

- **어떤 Tool이 "등록된" 상태인지**는 `@AiTool` 빈이 실제로 스캔됐는지에 달려 있습니다. 현재는 `DateTimeTools`, `SqlSyntaxTools`는 항상 등록되지만, `ShellExecTool`/`PythonExecTool`/`HttpCallTool`은 각자 `dstone.ai.tool.{shell,python,http}.enabled=false`(기본값)면 애초에 `@AiTool` 빈 자체가 안 뜰 가능성이 높습니다(정확한 활성화 방식은 각 Tool 클래스 확인 필요 — `@ConditionalOnProperty` 류로 빈 등록 자체를 막고 있다면 화이트리스트와 무관하게 처음부터 Tool 목록에 없음).
- **Agent/Workflow 쪽에서 Tool 사용 자체를 켰는지**도 별도입니다. `AgentDefinition.toolsEnabled()`(또는 `ChatController`의 요청별 override)가 `false`면 화이트리스트 허용 여부와 무관하게 그 호출에는 Tool이 아예 안 붙습니다.

**요약:** 지금은 "caller별 차등"이 꺼져 있는 상태이므로, Tool 사용이 켜진 Agent/Workflow는 등록된 Tool 전체(그리고 shell/python/http처럼 자체 enabled 플래그로 빈이 떠 있다면 그것도 포함)에 접근할 수 있습니다. 여러 SI 프로젝트가 이 엔진을 공유하기 시작하는 시점에는 `dstone.ai.tool.allowed-by-caller`를 실제로 채워 넣어야 의도한 격리가 동작합니다.

---

## 질문

parallelGroup에 대한 설명을 해줘. 그리고parallelGroup 테스트 할 수 있는 환경은 아니지 ?

---

## 답변

## parallelGroup 설명

`StepDefinition.parallelGroup`(문자열 필드)에 **같은 값**을 넣은 인접 step들을, `WorkflowExecutor`가 동시에 실행해주는 기능입니다. "순차/분기/병렬/루프 4패턴" 중 병렬 담당입니다.

**동작 방식** (`WorkflowExecutor.java`):

1. `run()`이 현재 step을 실행하기 전에 `parallelGroupOf(steps, step)`을 호출해서, 같은 `parallelGroup` 값을 가진 step들을 전체 목록에서 모읍니다(79번 줄). `parallelGroup`이 비어있으면 자기 자신 하나만 담긴 목록을 돌려줘 기존 순차 실행과 동일하게 동작합니다.
2. 그룹 크기가 2개 이상이면 `runParallel()`(171번 줄)이 각 step을 `CompletableFuture.supplyAsync(...)`로 동시에 던지고 `CompletableFuture.allOf(...).join()`으로 전부 끝날 때까지 기다립니다.
3. 결과는 각 step의 `outcome.text()`를 개행으로 이어붙인 하나의 문자열로 합치고, 하나라도 실패하면 전체를 실패(`allSuccess=false`)로 취급합니다.
4. 다음 step 결정은 **그룹의 마지막 step 기준**으로 합니다(83번 줄 주석) — 즉 그룹 내 각 step이 각자 `onSuccess`/`onFailure`를 가져도, 실제 분기 판단은 YAML상 그룹의 마지막 step에 적힌 값만 쓰입니다. 그룹 중간 step들의 `onSuccess`/`onFailure`는 사실상 무시됩니다(주의할 부분).

`CompletableFuture.supplyAsync`는 기본 `ForkJoinPool.commonPool()`을 씁니다 — 별도 전용 스레드풀을 구성하지 않았습니다.

## 테스트 환경 여부

맞습니다, **지금은 실제로 검증해볼 수 있는 환경이 아닙니다.**

- `src/main/resources/workflows/` 안에는 `oracle-to-postgresql.yml` 하나뿐이고, 이건 `analyze → convert → validate → (실패시 fix 루프)`로 **순수 순차 + 루프** 패턴만 씁니다. `parallelGroup`을 쓰는 Workflow YAML이 프로젝트 전체에 하나도 없습니다.
- CLAUDE.md에 기록된 "2026-09-15 live-verification"도 이 순차 Workflow 하나만 sync/async로 검증한 기록이고, 병렬 분기는 실행해본 적이 없습니다.
- `src/test/` 디렉터리 자체가 없습니다(`spring-boot-starter-test` 의존성은 pom.xml에 있지만 실제 테스트 코드가 전무). 즉 JUnit으로 `WorkflowExecutor.runParallel()`을 단위 테스트할 장치도 없는 상태입니다.

**실제로 테스트하려면** 다음이 필요합니다:
1. `parallelGroup`이 같은 값을 가진 step 2개 이상을 포함한 Workflow YAML 하나 새로 작성 (TOOL step끼리 묶으면 LLM 호출 비용/네트워크 의존 없이 로컬에서 바로 검증 가능 — 예: `validateSqlSyntax`를 서로 다른 입력으로 두 번 부르는 임시 Workflow)
2. `WorkflowController`의 `/execute` 또는 `/submit`+`/status`로 실제 호출해보거나, `src/test/java`를 새로 만들어 `WorkflowExecutor`를 직접 단위 테스트
3. AGENT step을 포함해서 병렬 검증하고 싶다면 Anthropic API 키와 실제 호출 비용이 필요(TOOL만으로는 LLM 개입 없는 병렬 실행 자체는 검증 가능하지만, "AGENT 두 개가 진짜 동시에 LLM 호출하는지"까지 보려면 API 호출이 필요)

원하시면 최소한의 검증용 테스트 Workflow YAML(TOOL 2개를 parallelGroup으로 묶은)을 하나 만들어서 로컬에서 바로 돌려볼 수 있는 형태로 준비해드릴 수 있습니다.

---

## 질문

dstone-boot 의 화면에서 테스트 할 수 있도록 준비해줘.

---

## 답변

YAML 파싱 정상 확인했습니다. 준비 완료했습니다.

## 준비한 내용

**1. 새 Workflow YAML 추가** — `dstone-ai-engine/src/main/resources/workflows/parallel-test.yml`

LLM을 쓰는 AGENT step 없이 `validateSqlSyntax` TOOL step 두 개(`validate-a`, `validate-b`)만으로 구성했습니다. 둘 다 `parallelGroup: check`를 가지므로 `WorkflowExecutor`가 동시에 실행합니다. API 비용/네트워크 의존 없이 즉시 돌려볼 수 있습니다.

**2. dstone-boot 쪽은 코드 변경이 필요 없습니다** — 이미 범용 화면이 있습니다

`/ai/workflow.do`("Workflow 비동기 테스트" 화면, `WorkflowTestController`/`WorkflowTestService`)가 `workflowId`를 화면에서 직접 입력받아 어떤 Workflow든 `submit`→`status` 폴링으로 호출해볼 수 있도록 이미 만들어져 있어서, 새 Workflow YAML만 추가하면 바로 테스트 가능합니다.

**3. 테스트 방법**

dstone-ai-engine을 재기동한 뒤, dstone-boot 화면에서:
- `workflowId`: `parallel-test`
- `message`: `validate-a`가 검증할 SQL (예: `SELECT 1 FROM dual`)
- `variables`: `{"sqlB": "validate-b가 검증할 SQL"}` (예: `{"sqlB": "SELECT * FROM WRONG SYNTAX ("}`)

제출하면 jobId가 발급되고 2초 간격으로 상태를 폴링합니다. 두 SQL이 모두 문법상 맞으면 `SUCCESS`, 하나라도 틀리면 `FAIL`로 끝나면서 `[검증 결과]`에 어느 쪽이 실패했는지 텍스트가 붙어 나옵니다 — 두 step이 실제로 각자 독립적으로 실행/판정된다는 걸 확인할 수 있습니다. `variables`를 비우면 `{sqlB}` 토큰이 치환되지 않아 그대로 파싱 실패(`실패:` 응답)가 나니, 꼭 채워서 테스트해야 합니다.

**4. 문서 업데이트** — `docs/09.dstone-ai-engine.md` 9절(YAML 정의 작성법)에 이 새 예시 파일과 화면 사용법을 추가했습니다.

참고로 이 테스트는 "동시에 실행됐는지(코드 경로가 실제로 병렬 분기를 타는지)"와 "그룹 결과가 AND로 합쳐지고 마지막 step 기준으로 다음 분기가 결정되는지"는 확인할 수 있지만, 진짜 "동시에 실행되어 벽시계 시간이 절약됐는지"(타이밍 증명)까지 보려면 별도의 sleep성 테스트 Tool이 필요합니다 — 원하시면 그것도 추가해드릴 수 있습니다.

---

## 질문

이건 왜 이럴까? 2026-09-16 11:39:05  INFO [net.dstone.ai.common.config.ConfigCallLog] +->[CONTROLLER] {WorkflowController.execute(String[oracle-to-postgresql]WorkflowRequest[message=SELECT NVL( '0', 'AA' ) AA FROM TEST,sessionId=323a9fa2-3b1e-4a23-beff-a955cb2de083,variables=<null>], )}
2026-09-16 11:39:05  INFO [net.dstone.ai.common.config.ConfigCallLog] +----->[Runtime ] {AgentStepRunner.runAgent(String[sql-analysis-agent]String[323a9fa2-3b1e-4a23-beff-a955cb2de083], <null>, <null>, String[SELECT NVL( '0', 'AA' ) AA FROM TEST], )}
2026-09-16 11:39:05  INFO [net.dstone.ai.common.config.ConfigCallLog] +----->[Runtime ] {AgentExecutor.call(String[323a9fa2-3b1e-4a23-beff-a955cb2de083]<null>, AgentDefinition[allowedCallers=<null>,description=Oracle SQL의 Oracle 전용 문법을 분석한다(oracle-to-postgresql Workflow의 analyze step).,name=sql-analysis-agent,promptName=oracle-sql-analyzer,ragEnabled=false,toolsEnabled=false], <null>, String[SELECT NVL( '0', 'AA' ) AA FROM TEST], <null>, <null>, )}
2026-09-16 11:39:20  INFO [net.dstone.ai.common.config.ConfigCallLog] +----->[Runtime ] {AgentStepRunner.runAgent(String[sql-conversion-agent]String[323a9fa2-3b1e-4a23-beff-a955cb2de083], <null>, <null>, String[[원본 SQL]
SELECT NVL( '0', 'AA' ) AA FROM TEST

[변환 포인트]
1. NVL 함수 → CASE WHEN THEN END로 변환 필요, NULL을 대신 0으로 변환하기 위함], )}
2026-09-16 11:39:20  INFO [net.dstone.ai.common.config.ConfigCallLog] +----->[Runtime ] {AgentExecutor.call(String[323a9fa2-3b1e-4a23-beff-a955cb2de083]<null>, AgentDefinition[allowedCallers=<null>,description=분석된 Oracle SQL을 PostgreSQL SQL로 변환한다(oracle-to-postgresql Workflow의 convert step).,name=sql-conversion-agent,promptName=oracle-to-postgresql,ragEnabled=false,toolsEnabled=false], <null>, String[[원본 SQL]
SELECT NVL( '0', 'AA' ) AA FROM TEST

[변환 포인트]
1. NVL 함수 → CASE WHEN THEN END로 변환 필요, NULL을 대신 0으로 변환하기 위함], <null>, <null>, )}
2026-09-16 11:39:32  INFO [net.dstone.ai.common.config.ConfigCallLog] +----->[Runtime ] {ToolStepRunner.run(StepDefinition[id=validate,inputTemplate={"sql":"{previous}"},onFailure=fix,onSuccess=SUCCESS,parallelGroup=<null>,ref=validateSqlSyntax,type=TOOL]<null>, <null>, String[```sql
SELECT CASE WHEN '0' IS NULL THEN 'AA' ELSE '0' END AS AA FROM TEST
```], )}
2026-09-16 11:39:32  INFO [net.dstone.ai.common.config.ConfigCallLog] +----->[Runtime ] {ToolExecutor.call(<null>String[validateSqlSyntax], String[{"sql":"```sql\nSELECT CASE WHEN '0' IS NULL THEN 'AA' ELSE '0' END AS AA FROM TEST\n```"}], )}
2026-09-16 11:39:32  INFO [net.dstone.ai.common.config.ConfigCallLog] +----->[Tools ] {SqlSyntaxTools.validateSqlSyntax(String[```sql
SELECT CASE WHEN '0' IS NULL THEN 'AA' ELSE '0' END AS AA FROM TEST
```])}
2026-09-16 11:39:32  INFO [net.dstone.ai.common.config.ConfigCallLog] +----->[Runtime ] {AgentStepRunner.runAgent(String[sql-fix-agent]String[323a9fa2-3b1e-4a23-beff-a955cb2de083], <null>, <null>, String[```sql
SELECT CASE WHEN '0' IS NULL THEN 'AA' ELSE '0' END AS AA FROM TEST
```

[검증 결과] 실패: 문법 오류 - net.sf.jsqlparser.parser.TokenMgrException: Lexical error at line 1, column 2.  Encountered: '96' (96), after prefix "`"], )}
2026-09-16 11:39:32  INFO [net.dstone.ai.common.config.ConfigCallLog] +----->[Runtime ] {AgentExecutor.call(String[323a9fa2-3b1e-4a23-beff-a955cb2de083]<null>, AgentDefinition[allowedCallers=<null>,description=문법 검증에 실패한 변환 SQL을 검토 후 다시 작성한다(oracle-to-postgresql Workflow의 fix step).,name=sql-fix-agent,promptName=oracle-sql-reviewer,ragEnabled=false,toolsEnabled=true], <null>, String[```sql
SELECT CASE WHEN '0' IS NULL THEN 'AA' ELSE '0' END AS AA FROM TEST

---

## 답변

로그를 보면 원인이 명확하게 찍혀 있습니다.

## 무슨 일이 일어났나

`sql-conversion-agent`(promptName=`oracle-to-postgresql`)가 변환 결과를 이렇게 반환했습니다:

```
```sql
SELECT CASE WHEN '0' IS NULL THEN 'AA' ELSE '0' END AS AA FROM TEST
```
```

이 텍스트가 그대로 `validate` step의 `inputTemplate`(`{"sql":"{previous}"}`)에 꽂혀서 `SqlSyntaxTools.validateSqlSyntax`(JSQLParser)에 전달됐고, JSQLParser는 맨 앞의 백틱(`` ` ``)에서 바로 렉서 에러를 냅니다:

```
TokenMgrException: Lexical error at line 1, column 2. Encountered: '96' (96), after prefix "`"
```

즉 **마크다운 코드펜스(```sql ... ```)가 SQL 문자열에 그대로 섞여 들어가서 파서가 첫 글자부터 못 읽은 것**입니다.

## 왜 코드펜스가 붙었나

`prompts/oracle-to-postgresql/v1.st`의 [출력 형식] 규칙 22번을 보면 이미 명시돼 있습니다:

> 22. 설명, 인사말, **마크다운 코드펜스(```sql 등)를 절대 출력하지 말고**, 오직 변환된 PostgreSQL SQL 문자열만 반환하십시오.

프롬프트에 금지 규칙이 있는데도 이번 호출에서는 LLM이 그 지시를 지키지 않고 코드펜스를 붙여서 답했습니다. 이건 코드 버그가 아니라 **LLM이 프롬프트 지시를 100% 결정적으로 따르지는 않는다는 근본적인 한계**입니다 — 특히 "SQL 코드"처럼 생긴 결과물은 모델이 습관적으로 코드펜스로 감싸는 경향이 있어서, 아무리 "하지 말라"고 못박아도 가끔 새어 나옵니다.

## 지금 구조의 약한 지점

`AgentStepRunner`/`ToolStepRunner`는 Agent가 반환한 텍스트를 **아무 가공 없이 그대로** 다음 TOOL step의 입력으로 씁니다. 즉 "코드펜스를 쓰지 마라"는 프롬프트 지시 하나에 전적으로 의존하고 있고, 파이프라인 쪽에는 방어 로직이 전혀 없습니다. 사람이 짠 프롬프트 규칙은 깨질 수 있다는 전제로, TOOL step에 넘기기 전에 코드펜스를 벗겨내는 방어 코드를 한 겹 두는 게 안전합니다(예: `ToolStepRunner.renderToolInput()`에서 ```` ```(언어명)?\n...\n``` ```` 패턴을 벗겨내고 `{previous}`에 채우기).

## 덤으로 하나 더 짚을 부분

로그가 `sql-fix-agent` 호출 직전에서 끊겨 있는데, `sql-fix-agent`의 `promptName`은 `oracle-sql-reviewer`입니다. 이 프롬프트(`prompts/oracle-sql-reviewer/v1.st`) 내용을 보면:

> 당신은 **이미 문법 검증까지 통과한** PostgreSQL SQL을 최종 검토해서... "[변환된 SQL]\n(입력받은 SQL을 **그대로 출력**)"

즉 이 프롬프트는 "검증 실패 → 원인 보고 받고 실제로 SQL을 고치는" 용도가 아니라 "이미 성공한 SQL을 요약 보고하는" 용도로 쓰여 있습니다. `fix` step에서 이 프롬프트를 재사용하고 있으면, `[검증 결과] 실패: ...` 텍스트를 받아도 실제로 코드펜스를 벗기거나 SQL을 고치라는 지시가 프롬프트 안에 전혀 없어서, 같은 실패가 반복되다가 `maxIterations`(5) 초과 예외로 끝날 가능성이 있습니다. 이번 로그가 거기까지 갔는지 다음 줄을 보면 확인 가능합니다.

**정리하면 두 가지 문제가 겹쳐 있을 수 있습니다:**
1. (직접 원인) 변환 Agent가 규칙 22를 어기고 코드펜스를 출력 → 파서가 못 읽음
2. (재발 방지 실패 원인 후보) `fix` step에 물린 프롬프트가 "리뷰용"이라 실제 수정 지시가 없어서, 루프를 돌아도 안 고쳐질 수 있음

1번은 `ToolStepRunner`에 코드펜스 스트리핑 방어 코드를 추가하면 근본적으로 막을 수 있고, 2번은 `sql-fix-agent` 전용 프롬프트(실패 사유를 받아 실제로 SQL을 재작성하라는 지시 포함)를 따로 만드는 게 맞아 보입니다. 원하시면 둘 중 하나(또는 둘 다) 바로 고쳐드릴까요?

---

## 질문

2번에 대한 수정을 진행해줘

---

## 답변

이제 `sql-fix-agent`가 이 새 프롬프트를 쓰도록 연결하고, 버전 설정을 추가합니다.

---

## 답변

2번(수정 안 되는 fix-agent 프롬프트) 수정을 완료했습니다.

## 변경 내용

1. **새 프롬프트 추가** — `dstone-ai-engine/src/main/resources/prompts/oracle-sql-fixer/v1.st`
   - "이미 통과한 SQL을 리뷰/요약"하는 게 아니라, 입력에 붙어 오는 `[검증 결과] 실패: ...` 사유를 근거로 **실제로 SQL을 고쳐서 반환**하도록 지시
   - 코드펜스(백틱)나 세미콜론 다중 문장처럼 흔한 실패 원인을 명시적으로 다루도록 규칙 추가(지난번 로그의 원인이었던 코드펜스 케이스도 커버)
   - 오류와 무관한 부분은 원본 유지하라고 명시(전체 재작성 방지)
   - `toolsEnabled: true`를 활용해 최종 답변 전 `validateSql`로 자체 재검증하라는 지시 포함(converter 프롬프트의 규칙 21과 동일한 패턴)
   - 최종 출력엔 설명/인사말/코드펜스 금지(converter 프롬프트의 규칙 22와 동일)

2. **`agents/sql-convert.yml`** — `sql-fix-agent`의 `promptName`을 `oracle-sql-reviewer` → `oracle-sql-fixer`로 변경, description도 "검토 후 다시 작성" → "실패 사유에 맞춰 실제로 고쳐 쓴다"로 정정

3. **`conf/application.yml`** — `dstone.ai.prompt.versions`에 `oracle-sql-fixer: v1` 추가(기존 컨벤션과 동일하게 명시적 버전 등록)

`oracle-sql-reviewer` 프롬프트 파일과 그 버전 설정은 그대로 남겨뒀습니다(현재는 어디서도 참조하지 않지만, "이미 통과한 SQL을 요약 보고"하는 용도 자체는 나중에 별도 리뷰 기능에 재사용될 수 있어서 임의로 지우지 않았습니다 — 필요 없다고 판단되면 말씀해주시면 정리하겠습니다).

Java 코드는 건드리지 않았고 리소스(YAML/프롬프트)만 바꿨으니, dstone-ai-engine을 재기동하면 바로 반영됩니다. 재현했던 케이스(`SELECT NVL( '0', 'AA' ) AA FROM TEST`)로 `oracle-to-postgresql` Workflow를 다시 돌려서 fix 루프가 실제로 코드펜스를 벗겨내고 통과하는지 확인해보시면 됩니다.
