# localcommandcaveatCa

> 생성일시: 2026-09-10 16:00

## 질문

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

---

## 질문

<command-name>/clear</command-name>
            <command-message>clear</command-message>
            <command-args></command-args>

---

## 질문

dstone-boot 에 dstone-ai-engine 과 연동하는 메뉴를 붙이려고 해. 계획을 검토해줘.

1. 백엔드
아래와 같은 두 패키지를 만들고 백엔드를 구현하려고 해.
net.dstone.boot.controller 
net.dstone.boot.service
연동은 BaseService 를 상속한 Service 티어에서 getRestTemplate() 을 이용해서 연동하는 걸로 하고.

2. 프론트엔드
아래에 UI를 만들까 해.
src/main/webapp/WEB-INF/views/ai
화면 형식은 Ollama Web UI Lite 와 유사하게 하면 어떨까 해.

---

## 답변

Let me look at the existing dstone-boot patterns (BaseService, controllers) and dstone-ai-engine's API surface before reviewing the plan.

---

## 답변

조사한 결과를 바탕으로 계획을 검토했습니다. 구조 자체는 방향이 맞지만, 실제 구현 전에 짚어야 할 지점이 몇 개 있습니다.

## 백엔드

**1. 패키지 구조가 기존 컨벤션과 다릅니다**
현재 dstone-boot는 `net.dstone.boot.controller` / `net.dstone.boot.service`처럼 계층별 flat 패키지를 쓰지 않고, `analyzer.controller`/`analyzer.service`, `sample.user.controller`/`sample.user.service`처럼 **기능 단위로 패키지를 묶는** 컨벤션입니다. AI 연동도 하나의 독립 기능(analyzer와 가장 유사한 성격)이므로 `net.dstone.boot.ai.controller`, `net.dstone.boot.ai.service`(필요시 `.vo`)로 묶는 게 기존 코드와 일관됩니다.

**2. `BaseService.getRestTemplate()` 선택은 적절하나, 스트리밍과는 안 맞습니다**
`RestFulUtil.getRestTemplate()`은 `RestTemplate` 기반이라 응답을 통으로 받은 뒤 반환합니다. 그런데 dstone-ai-engine의 `POST /api/ai/chat`(`ChatController.java`)은 애초에 `spec.user(...).call().content()`로 **스트리밍이 아닌 완전응답 API**입니다. "Ollama Web UI Lite"류 UI의 핵심 특징이 토큰 단위 스트리밍인데, 지금 엔진 쪽에 스트리밍 엔드포인트가 없어서 RestTemplate로 그대로 연동해도 어차피 스트리밍은 안 됩니다. 즉 두 가지 선택지가 있습니다:
   - (a) 지금 범위는 "로딩 스피너 → 완성된 답변 한 번에 표시"로 가고 스트리밍은 다음 단계로 미룬다 → 계획하신 RestTemplate 방식 그대로 가능.
   - (b) 진짜 토큰 스트리밍 UX를 원한다 → dstone-ai-engine에 `chatClient.stream()` 기반 SSE 엔드포인트를 먼저 추가해야 하고, dstone-boot 쪽도 RestTemplate이 아니라 `WebClient`(리액티브) 또는 SSE 프록시가 필요합니다. `BaseService.getRestTemplate()` 패턴으로는 커버가 안 됩니다.

**3. 대화(sessionId) 연속성 처리가 계획에서 빠져 있습니다**
`ChatController`는 `HttpSession`에 `sessionId`가 있으면 그걸 쓰고, 없으면 요청의 `sessionId`, 그것도 없으면 새로 UUID를 발급합니다. dstone-boot → dstone-ai-engine 호출은 서버-서버 RestTemplate 호출이라 **쿠키가 전달되지 않으므로**, ai-engine 쪽 HttpSession은 사실상 매번 비어있는 새 세션입니다. 결국 멀티턴 대화가 이어지려면 **dstone-boot의 서비스 계층이 최초 응답의 `sessionId`를 (로그인 세션이든 별도 대화 테이블이든) 직접 보관했다가 다음 요청마다 명시적으로 실어 보내야** 합니다. 이 부분을 처음부터 설계에 넣지 않으면 "매 메시지가 새 대화로 취급되는" 버그로 나타납니다.

**4. ai-engine 주소를 하드코딩하면 안 됩니다**
dstone-boot와 dstone-ai-engine은 같은 kind 클러스터, 같은 네임스페이스(`dstone`)의 Pod이므로 클러스터 내부에서는 `http://dstone-ai-engine:8081`(k8s Service DNS)로 접근해야 합니다. `localhost`는 안 됩니다(같은 Pod이 아니라서). 기존 `DB_HOST`/`REDIS_HOST`처럼 `env.properties` + `application.yml`의 `${AI_ENGINE_HOST}:${AI_ENGINE_PORT}` 패턴으로 외부화하는 걸 권합니다. 지금 `interface:` 블록(naver/kakao/google)은 전부 외부 공개 URL이라 구조가 다르니 그대로 재사용하기보다 datasource류처럼 env 변수 기반으로 가는 게 맞습니다.

**5. 보안 설정은 크게 걸릴 게 없습니다**
`ConfigSecurity`가 CSRF를 전역 disable(`http.csrf(csrf -> csrf.disable())`, `ConfigSecurity.java:93`) 해뒀고, 동적 URL 권한체크는 로그인 세션 유무만 보므로 새 URL(`/ai/**.do`, `/views/ai/**`)은 로그인 필요 화면으로 자연스럽게 들어갑니다. 별도 permitAll 등록은 필요 없습니다(로그인 없이 열 게 아니라면).

## 프론트엔드

`src/main/webapp/WEB-INF/views/ai` 위치는 기존 `views/analyzer`, `views/sample/*` 컨벤션과 맞아서 문제없습니다. 다만 범위를 먼저 정해야 합니다: 지금 ai-engine이 제공하는 옵션은 `message/sessionId/promptName/ragEnabled/toolsEnabled/requiredTool`인데, RAG는 `dstone.ai.rag.enabled`가 꺼져 있으면(현재 기본값) 쓸 수 없고, Tool은 항상 켜져 있습니다(Phase 3, 샘플 Tool만 등록됨). "Ollama Web UI Lite 스타일"이 문서 업로드/RAG 토글까지 포함하는 풀 UI를 의미하는지, 아니면 1단계로 메시지 입출력 채팅창만 먼저 만드는 건지에 따라 화면 구성이 꽤 달라집니다.

스트리밍 여부를 먼저 정하는 게 나머지(백엔드 REST 클라이언트 선택, 프론트 렌더링 방식) 설계를 좌우하는 핵심 갈림길이라 먼저 확인하고 싶습니다.

---

## 답변

무엇을 더 명확히 하고 싶으신지 말씀해 주세요.

---

## 질문

아래의 답변을 확인하고 다시 계획해줘.  
  
  <백엔드>

  1. 패키지 구조가 기존 컨벤션과 다릅니다
  현재 dstone-boot는 net.dstone.boot.controller / net.dstone.boot.service처럼 계층별 flat 패키지를 쓰지 않고, analyzer.controller/analyzer.service,
  sample.user.controller/sample.user.service처럼 기능 단위로 패키지를 묶는 컨벤션입니다. AI 연동도 하나의 독립 기능(analyzer와 가장 유사한 성격)이므로 net.dstone.boot.ai.controller,
  net.dstone.boot.ai.service(필요시 .vo)로 묶는 게 기존 코드와 일관됩니다.
      ==>> 
      이와같이 수정.
      net.dstone.boot.ai.controller 
      net.dstone.boot.ai.service

  2. BaseService.getRestTemplate() 선택은 적절하나, 스트리밍과는 안 맞습니다
  RestFulUtil.getRestTemplate()은 RestTemplate 기반이라 응답을 통으로 받은 뒤 반환합니다. 그런데 dstone-ai-engine의 POST /api/ai/chat(ChatController.java)은 애초에
  spec.user(...).call().content()로 스트리밍이 아닌 완전응답 API입니다. "Ollama Web UI Lite"류 UI의 핵심 특징이 토큰 단위 스트리밍인데, 지금 엔진 쪽에 스트리밍 엔드포인트가 없어서
  RestTemplate로 그대로 연동해도 어차피 스트리밍은 안 됩니다. 즉 두 가지 선택지가 있습니다:
  - (a) 지금 범위는 "로딩 스피너 → 완성된 답변 한 번에 표시"로 가고 스트리밍은 다음 단계로 미룬다 → 계획하신 RestTemplate 방식 그대로 가능.
  - (b) 진짜 토큰 스트리밍 UX를 원한다 → dstone-ai-engine에 chatClient.stream() 기반 SSE 엔드포인트를 먼저 추가해야 하고, dstone-boot 쪽도 RestTemplate이 아니라 WebClient(리액티브) 또는 SSE
    프록시가 필요합니다. BaseService.getRestTemplate() 패턴으로는 커버가 안 됩니다.
      ==>>
      1. net.dstone.common.utils.WcUtil 을 WebClient(리액티브)를 이용하는걸로 리팩터링.
      2. net.dstone.common.biz.BaseService 내에 아래의 메서드 추가.
        protected WebClient getWebClient() {
            return WcUtil.getInstance().getWebClient();
        }
      3. BaseService 를 상속한 Service 티어에서 getWebClient() 을 이용해서 연동.
    

  3. 대화(sessionId) 연속성 처리가 계획에서 빠져 있습니다
  ChatController는 HttpSession에 sessionId가 있으면 그걸 쓰고, 없으면 요청의 sessionId, 그것도 없으면 새로 UUID를 발급합니다. dstone-boot → dstone-ai-engine 호출은 서버-서버 RestTemplate
  호출이라 쿠키가 전달되지 않으므로, ai-engine 쪽 HttpSession은 사실상 매번 비어있는 새 세션입니다. 결국 멀티턴 대화가 이어지려면 dstone-boot의 서비스 계층이 최초 응답의 sessionId를 (로그인
  세션이든 별도 대화 테이블이든) 직접 보관했다가 다음 요청마다 명시적으로 실어 보내야 합니다. 이 부분을 처음부터 설계에 넣지 않으면 "매 메시지가 새 대화로 취급되는" 버그로 나타납니다.
      ==>>
      1. dstone-boot 에서 해당 메뉴기능은 모두 로그인 이후에 사용하도록 SpringSecuriy설정.
      2. 세션값(키 : net.dstone.boot.common.web.SessionListener.USER_LOGIN_SESSION_KEY)을 요청파라메터 sessionId 에 넣어 보냄.

  4. ai-engine 주소를 하드코딩하면 안 됩니다
  dstone-boot와 dstone-ai-engine은 같은 kind 클러스터, 같은 네임스페이스(dstone)의 Pod이므로 클러스터 내부에서는 http://dstone-ai-engine:8081(k8s Service DNS)로 접근해야 합니다. localhost는
  안 됩니다(같은 Pod이 아니라서). 기존 DB_HOST/REDIS_HOST처럼 env.properties + application.yml의 ${AI_ENGINE_HOST}:${AI_ENGINE_PORT} 패턴으로 외부화하는 걸 권합니다. 지금 interface:
  블록(naver/kakao/google)은 전부 외부 공개 URL이라 구조가 다르니 그대로 재사용하기보다 datasource류처럼 env 변수 기반으로 가는 게 맞습니다.
      ==>>
      env.properties 에 변수화.
      
  5. 보안 설정은 크게 걸릴 게 없습니다
  ConfigSecurity가 CSRF를 전역 disable(http.csrf(csrf -> csrf.disable()), ConfigSecurity.java:93) 해뒀고, 동적 URL 권한체크는 로그인 세션 유무만 보므로 새 URL(/ai/**.do, /views/ai/**)은
  로그인 필요 화면으로 자연스럽게 들어갑니다. 별도 permitAll 등록은 필요 없습니다(로그인 없이 열 게 아니라면).

  <프론트엔드>

  src/main/webapp/WEB-INF/views/ai 위치는 기존 views/analyzer, views/sample/* 컨벤션과 맞아서 문제없습니다. 
  다만 범위를 먼저 정해야 합니다: 지금 ai-engine이 제공하는 옵션은 message/sessionId/promptName/ragEnabled/toolsEnabled/requiredTool인데, 
  RAG는 dstone.ai.rag.enabled가 꺼져 있으면(현재 기본값) 쓸 수 없고, Tool은 항상 켜져 있습니다(Phase 3, 샘플 Tool만 등록됨). 
      ==>> 무슨말인지 모르겠어
  "Ollama Web UI Lite 스타일"이 문서 업로드/RAG 토글까지 포함하는 풀 UI를 의미하는지, 아니면 1단계로 메시지 입출력 채팅창만 먼저 만드는 건지에 따라 화면 구성이 꽤 달라집니다.
      ==>> 일단 메시지 입출력 채팅창만.

---

## 답변

WcUtil을 WebClient 기반으로 리팩터링하면 기존 OAuth 로그인 연동(카카오/네이버/구글)이 이미 WcUtil을 쓰고 있을 가능성이 있어 먼저 사용처를 확인하겠습니다.

---

## 답변

좋습니다, 답변 다 확인했고 헷갈려하신 부분(RAG/Tool 옵션 얘기)은 "메시지 채팅창만" 선택으로 해소됐습니다 — 요약하면 dstone-ai-engine의 `/api/ai/chat`은 `message` 외에도 `ragEnabled`(문서검색 증강), `toolsEnabled`(도구호출) 같은 옵션이 있는데, 이번 1차 범위에서는 그 옵션들을 아예 안 쓰고 `message`/`sessionId`만 보내면 됩니다. 그 옵션들은 무시하면 되는 것이라 더 신경 안 쓰셔도 됩니다.

추가로 확인해보니 `net.dstone.common.biz.BaseController`에 이미 `/views/{a}/{b}` 범용 매핑이 있어서(`view2()` 메서드), 화면 오픈용 액션을 따로 만들 필요 없이 `/views/ai/chat`으로 요청하면 자동으로 `ai/chat.jsp`가 뜹니다. 그리고 `dstone-common`의 `WcUtil`은 지금 `TestBean.java`(샘플) 외엔 실사용처가 없어서(OAuth 로그인들은 안 씀) WebClient로 리팩터링해도 영향 범위가 작습니다. `spring-boot-starter-webflux`도 이미 dstone-common pom에 "RestTemplate 대신 WebClient 사용 목적"이라는 주석과 함께 들어가 있어 의존성 추가도 필요 없습니다.

최종 계획을 정리하면 다음과 같습니다.

## 백엔드

**dstone-common**
- `net.dstone.common.utils.WcUtil` — 기존 `Bean`/`execute()` API는 그대로 두고(하위 호환), `getInstance()` 싱글톤 + `getWebClient()`(reactor-netty 기반 `WebClient`, `RestFulUtil`과 동일한 커넥션타임아웃/응답타임아웃 값 적용)를 추가.
- `net.dstone.common.biz.BaseService` — `protected WebClient getWebClient() { return WcUtil.getInstance().getWebClient(); }` 추가.

**dstone-boot**
- `net.dstone.boot.ai.vo` — `AiChatRequestVo`/`AiChatResponseVo` (ai-engine의 `ChatRequest`/`ChatResponse` record와 JSON shape만 맞춘 자체 VO, 모듈 간 클래스 의존 없이 REST 계약으로만 연동).
- `net.dstone.boot.ai.service.AiChatService extends net.dstone.boot.common.biz.BaseService`
  - `HttpSession`의 `SessionListener.USER_LOGIN_SESSION_KEY` → `CustomUserDetails.getUsername()`을 `sessionId`로 사용(로그인 사용자당 대화 하나가 계속 이어짐).
  - `getWebClient().post().uri(...).bodyValue(...).retrieve().bodyToMono(AiChatResponseVo.class).block()`로 ai-engine `/api/ai/chat` 호출. (서블릿 스레드에서 `.block()` — 지금 단계는 리액티브 컨트롤러로 안 바꾸고 HTTP 클라이언트만 WebClient로 교체하는 것. 나중에 스트리밍으로 갈 때 컨트롤러까지 바꾸면 됨.)
- `net.dstone.boot.ai.controller.AiChatController extends net.dstone.boot.common.biz.BaseController`
  - `/ai/chat/sendMessage.do` (AJAX POST, jsonView) 하나만. 화면 오픈은 `/views/ai/chat` 범용 매핑으로 커버.
- `env.properties`: `AI_ENGINE_HOST`, `AI_ENGINE_PORT` 추가.
- `application.yml` (로컬 conf 및 `k8s/configmap.yaml` 양쪽): `interface.ai-engine.base-url: http://${AI_ENGINE_HOST}:${AI_ENGINE_PORT}` 형태로 추가. 로컬 실행은 `localhost:8081`, k8s는 `dstone-ai-engine:8081`(같은 네임스페이스 Service DNS)로 값만 다르게.
- `ConfigSecurity` 변경 없음(로그인 필요 화면으로 자연 편입, CSRF 이미 disable).

## 프론트엔드
- `src/main/webapp/WEB-INF/views/ai/chat.jsp` — 메시지 입력창 + 대화 리스트만 있는 단일 채팅창(사이드바 대화목록/RAG업로드/Tool토글 없음), `common/header.jsp`/`top.jsp`/`left.jsp` 레이아웃 재사용.
- `common/left.jsp`에 메뉴 항목 추가.
- 채팅 JS(`assets/js` 또는 `ai` 전용 정적 경로) — `sendMessage.do`로 AJAX POST, 응답 오면 리스트에 추가. 스트리밍이 아니므로 전송 중 로딩 인디케이터만 표시.

이대로 구현 시작할까요?

---

## 질문

ragEnabled(문서검색증강), toolsEnabled(도구호출) 을 안쓴다는말이 이해가 가지 않는데...
ragEnabled, toolsEnabled 등은 화면에서 선택적으로(라디오버튼 혹은 체크박스등) 선택해서 서버로 요청하면 되는 것 아닌가?

---

## 답변

확인해보니 제가 틀렸습니다. CLAUDE.md 문서의 "기본값 false"라는 설명만 보고 판단했는데, 실제 이 환경의 `dstone-ai-engine/conf/application.yml:160`에는 `dstone.ai.rag.enabled: true`로 **이미 켜져 있고** pgvector/Ollama 설정까지 다 들어가 있습니다. 죄송합니다, 잘못된 전제로 말씀드렸습니다.

---

## 답변

맞습니다, 정정합니다. `dstone-ai-engine/conf/application.yml:160` 확인해보니 `dstone.ai.rag.enabled: true`이고 Postgres(pgvector)/Ollama(bge-m3) 설정까지 실제로 다 붙어 있는 상태입니다. 이전에 제가 "RAG는 기본 꺼짐"이라고 한 건 CLAUDE.md 문서의 일반 설명만 보고 이 환경의 실제 conf를 확인 안 한 채 말씀드린 틀린 전제였습니다.

그러면 `ragEnabled`, `toolsEnabled` 둘 다 지금 바로 동작하는 옵션이 맞고, 체크박스로 노출하는 게 자연스럽습니다. 두 옵션 성격은 이렇습니다:

- **`toolsEnabled`**: 켜면 LLM이 필요할 때 스스로 Tool을 호출(`tool_choice=auto`). 지금은 샘플 Tool(`DateTimeTools.getCurrentDateTime`, RAG가 켜져 있으니 `RetrievalTools.searchKnowledgeBase`도 자동 등록됨) 2개가 붙어있는 상태.
- **`ragEnabled`**: 켜면 질문마다 무조건 먼저 벡터DB를 검색해서 컨텍스트로 끼워 넣음(`QuestionAnswerAdvisor`). 다만 이건 검색 대상 문서가 pgvector에 **적재되어 있어야** 의미가 있는데, 문서 적재는 `ChatController`가 아니라 별도 `RagController`(`POST /api/ai/rag/documents` 멀티파트 업로드)를 통해서만 가능합니다. 체크박스 자체는 지금 넣어도 되지만, 문서 업로드 화면이 없으면 검색할 게 없어 사실상 빈 컨텍스트로 동작합니다.

지금 pgvector에 이미 적재된 문서가 있는지는 확인 안 해봤습니다.

---

## 답변

무엇을 더 명확히 하고 싶으신지 말씀해 주세요.

---

## 질문

내생각에는 ai 도 분석툴(analyzer) 메뉴처럼 별도의 UI세트(자체메뉴세트)를 갖추는게 어떨까 싶어.
왜냐하면 ai 관련해서 추가적인 기능이 많이 붙을 것 같거든.
당장은 자체메뉴세트는 메인, 채팅창, 문서업로드 이 세개만 가져가고 추가적인 기능이 붙기 편하게 구성됨이 어떨까 싶어.
이 의견을 고려해서 다시 계획해줘

---

## 답변

analyzer가 자체 메뉴세트를 어떻게 구성했는지 그 패턴을 그대로 참고하기 위해 analyzer의 뷰/정적자원/컨트롤러 구조를 확인하겠습니다.

---

## 답변

좋은 지적입니다. analyzer의 구조를 그대로 보니 이해가 명확해졌습니다 — analyzer는 메인 앱의 `common/header.jsp`/`left.jsp`를 재사용하지 않고, **완전히 독립된 레이아웃**(`views/analyzer/common/{head,header,footer}.jsp`, 자체 nav)과 **독립된 정적자원 트리**(`webapp/analyzer/{images,assets,slickGrid}`)를 갖고 있고, 메인 앱의 `left.jsp`에는 진입점 링크 하나(`분석툴` → `/views/analyzer/index`)만 있습니다. 내부 이동은 `defaultLink.do?defaultLink=analyzer/xxx/yyy` 방식으로 analyzer 자체 헤더 nav가 처리합니다. AI도 이 패턴을 그대로 따르면 됩니다.

## 수정된 최종 계획

**백엔드 패키지** (analyzer와 동일한 기능단위 구조)
- `net.dstone.boot.ai.controller.ChatController` — `@RequestMapping("/ai/chat/*")` → `/ai/chat/sendMessage.do`
- `net.dstone.boot.ai.controller.DocumentController` — `@RequestMapping("/ai/document/*")` → `/ai/document/uploadDocument.do`(멀티파트), `/ai/document/deleteDocument.do`
- `net.dstone.boot.ai.service.ChatService`, `net.dstone.boot.ai.service.DocumentService` (둘 다 `BaseService.getWebClient()`로 ai-engine 연동)
- `net.dstone.boot.ai.vo.*` (필요한 만큼)
- (WcUtil/BaseService WebClient 추가, env.properties/application.yml AI_ENGINE_HOST·PORT 등 이전에 합의된 백엔드 항목은 그대로 유지)

**프론트엔드 — AI 전용 메뉴세트**
- `views/ai/index.jsp` — AI 메인(허브) 페이지. 컨트롤러 없이 `BaseController`의 `/views/{a}/{b}` 범용매핑으로 커버.
- `views/ai/chat/chat.jsp` — 채팅창(message/ragEnabled/toolsEnabled 체크박스)
- `views/ai/document/document.jsp` — 문서 업로드 화면
- `views/ai/common/{head,header,footer}.jsp` — analyzer처럼 자체 레이아웃. header의 nav에 메인/채팅/문서업로드 3개 항목(향후 기능 추가 시 여기 탭만 늘리면 됨).
- `webapp/ai/assets/{js,css}` — 자체 정적자원 트리(공용 assets 재사용 안 함).
- 메인 앱 `common/left.jsp`에는 진입점 링크 1개만 추가: `AI` → `/views/ai/index`.

**막히는 지점 하나**: `문서업로드` 화면에서 "지금까지 적재된 문서 목록"을 보여주려고 하는데, dstone-ai-engine의 `RagController`엔 업로드(POST)/삭제(DELETE)/검색(POST)만 있고 **적재된 sourceId 목록을 조회하는 API가 없습니다.** 그래서 두 가지 중 골라야 합니다:
1. **목록 없이 최소구성** — 업로드 폼(sourceId 직접 입력) + 삭제 폼(sourceId 직접 입력)만. 지금 뭐가 올라가 있는지는 화면에서 안 보임.
2. **dstone-boot에 자체 메타데이터 테이블 추가**(dstone-batchadmin이 `TB_BATCH_JOB`으로 배치서버의 실제 Job과 별개로 메타정보를 자체 관리하는 것과 같은 패턴) — 업로드할 때마다 `TB_AI_DOCUMENT`류 테이블에 sourceId/파일명/업로드일시를 기록해서 목록 그리드를 보여줌. 다만 이 테이블은 pgvector의 실제 적재 상태와 별개로 관리되는 것이라(싱크가 안 맞을 수 있음), 그 점 감안해야 합니다.

어느 쪽으로 갈까요?

---

## 질문

2번으로 가면 될 것 같아. 그런데 채팅은 스트리밍 방식으로 갈 수 없는건가?

---

## 답변

네, 가능합니다 — 오히려 지난번에 정하신 WebClient(리액티브) 선택이 스트리밍의 전제조건이라 이미 절반은 준비된 셈입니다. RestTemplate이었다면 스트리밍이 원천적으로 막혔겠지만, WebClient는 `Flux<String>`(SSE)을 그대로 다룰 수 있습니다.

**필요한 변경 (3개 모듈에 걸침):**

1. **dstone-ai-engine** — 지금 `/api/ai/chat`은 `.call().content()`(완전응답)뿐이라, 스트리밍용 엔드포인트를 하나 추가해야 합니다. 기존 엔드포인트는 그대로 두고(다른 SI 프로젝트가 이미 그 계약에 의존할 수 있으므로 비파괴적으로), `POST /api/ai/chat/stream`을 새로 만들어 `chatClient...stream()` → `Flux<String>`을 SSE(`text/event-stream`)로 반환. `promptName`/`ragEnabled`/`toolsEnabled` 로직은 스트리밍에도 그대로 적용 가능합니다(Spring AI가 지원).
2. **dstone-boot `ChatService`** — `getWebClient().post().uri(".../api/ai/chat/stream").accept(TEXT_EVENT_STREAM).retrieve().bodyToFlux(String.class)`로 수신.
3. **dstone-boot `ChatController`** — `/ai/chat/sendMessage.do` 액션이 `Flux<String>`을 `produces = TEXT_EVENT_STREAM_VALUE`로 그대로 반환(dstone-common이 webflux를 물고 있어 Spring MVC 컨트롤러에서도 reactive 타입 반환이 지원됨). 다만 이 액션은 다른 analyzer류 액션처럼 `ModelAndView`/jsonView 패턴을 안 따르는 예외적인 케이스가 됩니다 — 스트리밍이라 어쩔 수 없는 부분입니다.
4. **프론트엔드** — `EventSource`는 GET만 지원해서 `message` 본문을 못 실어 보내니, `fetch()` POST + `response.body.getReader()`로 청크를 읽어 토큰 단위로 화면에 붙이는 방식(ChatGPT류 웹앱들이 쓰는 방식)으로 갑니다.

세션/로그인 세션ID 처리, ragEnabled/toolsEnabled 체크박스, 문서업로드+자체 메타데이터 테이블(2번 확정)은 스트리밍 여부와 무관하게 그대로 유지됩니다.

## 최종 계획 (전체 정리)

**dstone-common**
- `WcUtil`에 `getInstance()`/`getWebClient()` 추가(기존 `Bean`/`execute()` API는 유지).
- `net.dstone.common.biz.BaseService`에 `getWebClient()` 추가.

**dstone-ai-engine**
- `ChatController`에 `POST /api/ai/chat/stream` 추가(SSE, 기존 `/api/ai/chat`은 유지).

**dstone-boot — 백엔드**
- `net.dstone.boot.ai.controller.{ChatController, DocumentController}`
  - `/ai/chat/*` → `sendMessage.do`(SSE 스트리밍)
  - `/ai/document/*` → `uploadDocument.do`(멀티파트), `deleteDocument.do`, `listDocument.do`
- `net.dstone.boot.ai.service.{ChatService, DocumentService}` — `BaseService.getWebClient()`로 ai-engine 연동.
- `net.dstone.boot.ai.vo.*`, `net.dstone.boot.ai.dao.DocumentDao`(`sqlSessionCommon` 사용 — 로그인/관리자 메타데이터와 같은 datasource) + `sqlmap/ai/DocumentDao.xml` + `schema/04-create-table-mysql-dstone-ai.sql`(`TB_AI_DOCUMENT`: sourceId, 파일명, 업로드일시, 업로더 등).
- `env.properties`/`application.yml`(로컬+`k8s/configmap.yaml`)에 `AI_ENGINE_HOST`/`AI_ENGINE_PORT` 및 `interface.ai-engine.base-url`.
- 세션ID: 로그인 사용자(`SessionListener.USER_LOGIN_SESSION_KEY` → `CustomUserDetails.getUsername()`)를 ai-engine `sessionId`로 사용.

**dstone-boot — 프론트엔드 (AI 전용 메뉴세트, analyzer와 동일 패턴)**
- `views/ai/index.jsp`(메인/허브), `views/ai/chat/chat.jsp`, `views/ai/document/document.jsp`
- `views/ai/common/{head,header,footer}.jsp` — 자체 레이아웃, nav에 메인/채팅/문서업로드 3탭(향후 기능은 탭만 추가).
- `webapp/ai/assets/{js,css}` — 자체 정적자원.
- `common/left.jsp`에 진입점 링크 1개(`AI` → `/views/ai/index`) 추가.

이대로 구현 시작할까요?

---

## 질문

응 구현 시작해줘

---

## 답변

Now let me apply the WcUtil and BaseService edits.

---

## 답변

dstone-common과 dstone-ai-engine을 먼저 컴파일해서 지금까지 변경이 문제없는지 확인하겠습니다.

---

## 답변

dstone-common 빌드 성공. 이제 dstone-ai-engine 빌드를 확인하겠습니다.

---

## 답변

Good, this gives me a clear modern `TB_` style to follow. Now let's create the schema file, sqlmap, VO, DAO, service, and controller for the document feature, plus the chat feature.

---

## 답변

Now let's write the DocumentVo, sqlmap, DAO, service, and controllers for the AI feature.

---

## 답변

백엔드 컴파일 성공. 이제 `left.jsp`에 진입점을 추가하고 프론트엔드(자체 메뉴세트)를 만들겠습니다.

---

## 답변

3개 모듈 모두 컴파일/패키징 성공했습니다(`dstone-common`, `dstone-ai-engine`, `dstone-boot` 전부 `mvn clean package` 통과, WAR에 새 클래스/뷰/정적자원 모두 포함 확인). 구현 내용 정리합니다.

**dstone-common**
- `WcUtil`에 `getInstance()`/`getWebClient()` 추가 (기존 `Bean`/`execute()` API는 그대로 유지).
- `BaseService`에 `getWebClient()` 추가.

**dstone-ai-engine**
- `ChatController`: 기존 로직을 `buildRequestSpec()`으로 추출해 재사용하고, `POST /api/ai/chat/stream`(SSE, `Flux<String>`) 추가. 기존 `/api/ai/chat`은 그대로 유지.

**dstone-boot 백엔드** (`net.dstone.boot.ai.*`)
- `ChatController`/`ChatService` — `/ai/chat/sendMessage.do`, WebClient로 ai-engine 스트림을 그대로 릴레이. sessionId는 로그인 사용자ID(`CustomUserDetails.getUsername()`)를 사용.
- `DocumentController`/`DocumentService`/`DocumentDao` — `/ai/document/{uploadDocument,listDocument,deleteDocument}.do`. 업로드는 기존 `RequestUtil`/`FileUpUtil` 관례를 그대로 따름. `TB_AI_DOCUMENT`(`schema/04-create-table-mysql-dstone-ai.sql`, `sqlmap/ai/DocumentDao.xml`)에 자체 메타데이터 기록.
- `env.properties`/`application.yml`(로컬+`k8s/configmap.yaml`) — `AI_ENGINE_HOST`/`AI_ENGINE_PORT`, `interface.ai-engine.base-url` 추가.

**dstone-boot 프론트엔드** — `views/ai/{index.jsp, common/*, chat/chat.jsp, document/document.jsp}` + `webapp/ai/assets/{css,js}`(analyzer와 동일하게 자체 레이아웃/자체 정적자원). `left.jsp`에 진입점(`AI`) 1개 추가. 채팅은 `fetch()` + SSE 파싱으로 토큰 스트리밍.

**아직 남은 수동 작업**
1. `sampleDB`에 `schema/04-create-table-mysql-dstone-ai.sql` 직접 실행해서 `TB_AI_DOCUMENT` 테이블 생성해야 합니다(다른 schema 파일들과 동일하게 자동실행 아님).
2. `conf/env.properties`의 `AI_ENGINE_HOST`/`AI_ENGINE_PORT`는 로컬 값(`localhost:8081`)으로 넣어뒀습니다. k8s 배포 시엔 원래도 `env.properties` 자체가 ConfigMap/Secret으로 안 들어가 있는 기존 공백이 있어서(DB_HOST 등도 마찬가지), 이 부분은 이번 작업과 별개로 언젠가 정리가 필요합니다.
3. 실제 서버 띄워서 로그인 후 `AI` 메뉴 → 채팅/문서업로드 동작은 아직 브라우저로 확인 못 했습니다 — 인프라(DB/Redis/ai-engine) 기동 여부를 알려주시면 같이 띄워서 확인해볼 수 있습니다.

---

## 질문

2026-09-10 03:45:50  INFO [net.dstone.common.exception.resolver.DsExceptionResolver] net.dstone.common.exception.resolver.DsExceptionResolver.doResolveException() has been called !!!
java.lang.NullPointerException: Cannot invoke "org.springframework.context.ApplicationContext.getBean(java.lang.Class)" because "net.dstone.common.utils.SpringUtil.context" is null
    at net.dstone.common.utils.SpringUtil.getBean(SpringUtil.java:18)
    at net.dstone.common.utils.FileUpUtil.<init>(FileUpUtil.java:32)
    at net.dstone.common.utils.RequestUtil.init(RequestUtil.java:79)
    at net.dstone.common.utils.RequestUtil.<init>(RequestUtil.java:51)
    at net.dstone.boot.ai.controller.DocumentController.uploadDocument(DocumentController.java:38)
    at java.base/jdk.internal.reflect.DirectMethodHandleAccessor.invoke(DirectMethodHandleAccessor.java:103)

---

## 답변

Let me rebuild dstone-boot to confirm this compiles cleanly.

---

## 답변

원인 찾았습니다 — 제가 만든 기능의 버그가 아니라 기존에 있던 잠재 버그였고, 이번에 문서업로드가 처음으로 그 경로를 건드려서 드러난 것입니다.

**원인**: 이 프로젝트는 `net.dstone.boot`만 `@ComponentScan`하고(`DstoneBootApplication.java`), `dstone-common`의 클래스는 패키지 전체 스캔이 아니라 `Config.java`의 `@Import` 화이트리스트에 명시된 것만 빈으로 등록합니다. 그런데 `net.dstone.common.utils.SpringUtil`이 이 목록에 빠져있었습니다. `SpringUtil`은 파일업로드 파싱을 담당하는 `FileUpUtil`이 `ConfigProperty` 빈을 얻으려고 내부적으로 쓰는 클래스인데(`SpringUtil.getBean(ConfigProperty.class)`), 빈으로 등록된 적이 없으니 `ApplicationContextAware.setApplicationContext()`가 한 번도 호출되지 않아 정적 필드 `context`가 계속 null이었던 겁니다. 지금까지 dstone-boot의 어떤 기능도 실제 요청에서 멀티파트 업로드를 실행한 적이 없어서(그래서 `FileUpUtil`이 인스턴스화된 적이 없어서) 드러나지 않았던 것뿐입니다.

**수정**: `net.dstone.boot/common/config/Config.java`의 `@Import` 목록에 `net.dstone.common.utils.SpringUtil.class` 추가했습니다. `dstone-boot` 재빌드/재패키징 확인했습니다(`mvn clean package` 통과).

서버 재기동 후 문서업로드 다시 시도해 보시겠어요?

---

## 질문

<task-notification>
<task-id>bxu2f8nuc</task-id>
<tool-use-id>toolu_01Y3DmwRziusZGaHSYEb8q9c</tool-use-id>
<output-file>/tmp/claude-1000/-app-dstone/0c6403ff-b54e-4f61-ba18-f2bcfb0e6b27/tasks/bxu2f8nuc.output</output-file>
<status>failed</status>
<summary>Background command "grep -rn "@ComponentScan" /app/dstone/dstone-boot/src/main/java --include=*.java
echo "=== spring.components (indexed) in dstone-common? ==="
find /app/dstone/dstone-common -iname "spring.components" -not -path "*/target/*"
find / -iname "spring.components" -path "*dstone-common*" 2&gt;/dev/null
echo "=== spring-context-indexer dependency? ==="
grep -n "context-indexer" /app/dstone/dstone-common/pom.xml" failed with exit code 1</summary>
</task-notification>

---

## 답변

(참고: 방금 알림은 앞서 제가 백그라운드로 돌려둔 조사용 grep/find 명령이 뒤늦게 끝난 것뿐입니다 — exit code 1은 단순히 검색 결과가 없었다는 의미이고, 원인은 이미 다른 방법으로 찾아서 `Config.java`에 반영·빌드까지 끝냈습니다. 추가로 달라지는 건 없습니다.)

재기동 후 문서업로드 다시 시도해 보시고, 결과 알려주세요.

---

## 질문

2026-09-10 03:58:10  INFO [net.dstone.boot.common.config.ConfigAspect] +->[CONTROLLER] {DocumentController.uploadDocument()}
2026-09-10 03:58:10  INFO [net.dstone.common.exception.resolver.DsExceptionResolver] net.dstone.common.exception.resolver.DsExceptionResolver.doResolveException() has been called !!!
org.springframework.web.server.ResponseStatusException: 400 BAD_REQUEST "SOURCE_ID와 업로드 파일은 필수입니다."
    at net.dstone.boot.ai.controller.DocumentController.uploadDocument(DocumentController.java:42)
    at java.base/jdk.internal.reflect.DirectMethodHandleAccessor.invoke(DirectMethodHandleAccessor.java:103)
    at java.base/java.lang.reflect.Method.invoke(Method.java:580)
    at org.springframework.aop.support.AopUtils.invokeJoinpointUsingReflection(AopUtils.java:359)
    at org.springframework.aop.framework.ReflectiveMethodInvocation.invokeJoinpoint(ReflectiveMethodInvocation.java:190)
    at org.springframework.aop.framework.ReflectiveMethodInvocation.proceed(ReflectiveMethodInvocation.java:158)
    at org.springframework.aop.aspectj.MethodInvocationProceedingJoinPoint.proceed(MethodInvocationProceedingJoinPoint.java:82)
    at net.dstone.boot.common.config.ConfigAspect.doControllerProfiling(ConfigAspect.java:52)
    at java.base/jdk.internal.reflect.DirectMethodHandleAccessor.invoke(DirectMethodHandleAccessor.java:103)
    at java.base/java.lang.reflect.Method.invoke(Method.java:580)
    at org.springframework.aop.aspectj.AbstractAspectJAdvice.invokeAdviceMethodWithGivenArgs(AbstractAspectJAdvice.java:648)
    at org.springframework.aop.aspectj.AbstractAspectJAdvice.invokeAdviceMethod(AbstractAspectJAdvice.java:630)
    at org.springframework.aop.aspectj.AspectJAroundAdvice.invoke(AspectJAroundAdvice.java:70)
    at org.springframework.aop.framework.ReflectiveMethodInvocation.proceed(ReflectiveMethodInvocation.java:168)

---

## 답변

Let's rebuild to confirm nothing is broken, then explain the fix.

---

## 답변

빌드 성공했습니다. 두 번째 원인도 찾았습니다.

**원인**: Spring Boot의 기본 멀티파트 리졸버(`StandardServletMultipartResolver`)가 켜져 있으면, 컨트롤러에 도달하기 전에 `DispatcherServlet`이 먼저 `request.getParts()`로 요청 바디를 통째로 소비해버립니다. 그런데 이 프로젝트는 파일업로드를 자체 `RequestUtil`/`FileUpUtil`(commons-fileupload)로 직접 파싱하는 구조라, `FileUpUtil`이 같은 요청의 `InputStream`을 다시 읽으려 하면 이미 비어있어서 SOURCE_ID(폼 필드)도 파일도 둘 다 못 읽었던 겁니다 — 에러 메시지에서 "SOURCE_ID와 업로드 파일이 둘 다" 필수라고 나온 게 그 증거입니다. 이것도 지금까지 이 코드베이스의 어떤 기능도 실제 파일업로드를 실행해본 적이 없어서 드러나지 않았던 잠재 버그입니다.

**수정**: `conf/application.yml`(+ `k8s/configmap.yaml`)에 `spring.servlet.multipart.enabled: false` 추가해서 Spring 기본 리졸버를 끄고, `FileUpUtil`이 요청을 독점 파싱하도록 했습니다. 재빌드 확인했습니다.

서버 재기동 후 다시 시도해 보시겠어요?
