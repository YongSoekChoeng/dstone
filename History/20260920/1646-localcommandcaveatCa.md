# localcommandcaveatCa

> 생성일시: 2026-09-20 16:46

## 질문

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

---

## 질문

<command-name>/clear</command-name>
            <command-message>clear</command-message>
            <command-args></command-args>

---

## 질문

나는 효율적이고 유연한 AI 엔진을 만들고 싶어.
java 기반의 spring ai를 사용해서 만들고 있어(dstone-ai-engine).
그런데 뭔가 조금 부족한 점이 있을 것 같아 수정해야 할 점이 있다면 말해줘.
하고자 하는 것을 조금 더 상세하게 설명하면 아래와 같아.
현재 샘플로 작성되어있는 워크플로어에 구애받지말고 범용적으로 판단해줘.

<기능 요구사항>
1. 설정 파일(yml)만으로 작업흐름(Workflow)를 정의할 수 있어야 한다.
2. Workflow 실행모듈에서 작업흐름 설정을 보고 agent을 호출해서 작업처리를 한다.
3. 개별 기능은 agent로 구성한다.
    - agent 는 LLM 과 통신을 통해서 명령을 수행한다.
    - agent 는 자발적으로 Tool Calling 도 가능해야 한다.
    - agent 는 설정에 따라 RAG 기능도 지원해야 한다.
    - agent 는 병렬처리 작업도 가능해야 한다.
4. Workflow, agent 이 유연하고 확장성이 있어야 한다.

<dstone-ai-engine 에 대한 의문점>
- dstone-ai-engine 을 사용해서 여러가지 업무에 적용할 수 있을만한 구조인가?
- 개별 STEP 마다 주고 받는 포맷이 명확했으면 좋겠는데 현재 구현된 구조에서는 그런것 같지 않는데 개선방법이 있을까?

---

## 답변

코드를 직접 훑어봤습니다 (`StepDefinition`/`StepType`/`AgentDefinition`, `WorkFlowExecutor`, `AgentStepRunner`/`ToolStepRunner`/`ApprovalStepRunner`, `AgentExecutor`, `ToolExecutor`). 전반적으로 이미 순차/분기/병렬/루프 4패턴, Tool 화이트리스트, RAG advisor, 멀티테넌트(`allowedCallers`)까지 잘 갖춰진 상태였고, 질문하신 두 가지 지점에서 구체적으로 걸리는 부분을 찾았습니다.

**1) "여러 업무에 범용적으로 쓸 만한 구조인가?"**
기본 골격(4패턴, YAML만으로 Workflow/Agent 추가)은 범용성 요구를 충족합니다. 다만 두 가지가 확장성을 갉아먹습니다.
- 모든 step 간 전달값이 결국 "문자열 하나(`primaryText`) + 평평한 전역 변수 맵"입니다. `StepOutput.data()`가 `variables.putAll()`로 **네임스페이스 없이** 병합되는데(`WorkFlowExecutor.mergeVariables`), 병렬 그룹의 두 step이 우연히 같은 키를 리턴하면 조용히 덮어씁니다. 업무가 다양해질수록(특히 병렬 step이 늘수록) 이 충돌 위험이 커집니다.
- 분기는 사실상 성공/실패 2지선다뿐입니다(`onSuccess`/`onFailure`). 업무 로직상 3갈래 이상 분기(예: 금액 구간별 처리)가 필요하면 SUPERVISOR를 여러 개 겹치는 우회밖에 없습니다.
- 동적 개수 팬아웃(리스트를 순회하며 항목별 병렬 처리)이 안 됩니다. `parallelGroup`은 YAML 작성 시점에 개수가 고정된 그룹만 지원합니다.

**2) "step 간 포맷이 불명확한데 개선 방법?"**
정확히 짚으셨습니다. 두 군데가 원인입니다.
- **TOOL step**: 이전 AGENT step이 순수 텍스트를 뱉는다는 계약뿐이라, 실제로는 LLM이 마크다운 코드펜스나 `[변환된 SQL]` 같은 레이블을 붙여 내려주는 일이 반복적으로 관찰됐고, 그걸 정규식으로 벗겨내는 방어 코드(`ToolStepRunner.stripLlmArtifacts`)가 생겼습니다. 이건 "계약이 강제되지 않는다"는 증상입니다. `SUPERVISOR`가 이미 `Verdict` record + `.entity(Verdict.class)`로 이 문제를 풀었으니(자유 텍스트가 아니라 JSON 스키마 강제), 데이터를 다음 step이 소비해야 하는 AGENT step에도 같은 패턴(`callForEntity(Class<T>)` 같은 범용 메서드)을 열어주면 정규식 방어 없이 구조적으로 해결됩니다.
- **TOOL step 성공/실패 판정**: 응답 텍스트가 `"실패:"`로 시작하는지 문자열 매칭으로 판정합니다(`Constants.Outcome.FAIL_PREFIX`). Tool 작성자가 이 접두사 컨벤션을 깜빡하면 컴파일도 안 걸리고 조용히 "성공"으로 처리됩니다. `boolean`을 실어 나르는 작은 결과 타입(또는 실패 시 예외)으로 바꾸면 이 매직 스트링 의존을 없앨 수 있습니다.

**우선순위 제안**: SUPERVISOR의 `Verdict` 패턴을 일반 AGENT step으로 확장(구조화 출력)하는 게 가장 임팩트가 크고, `data` 병합 시 step id로 네임스페이싱하는 건 적은 변경으로 충돌 위험을 없앨 수 있습니다. TOOL 성공/실패 문자열 접두사 교체는 그다음으로 봐도 됩니다. 동적 팬아웃/다지 분기는 지금 당장 필요한 업무가 나오기 전엔 굳이 먼저 만들 필요는 없어 보입니다.

이 중에서 어떤 것부터 실제로 손댈지 정해주시면 그것부터 구현하겠습니다 — 임의로 넷 다 한꺼번에 바꾸진 않겠습니다.

---

## 질문

Step 에서 Tool타입 이 필요한가? Agent 타입으로 Tool 콜링이 될 수도 있을 것 같은데...

---

## 답변

둘은 같은 일을 하는 게 아니라서 TOOL 타입은 남겨두는 게 맞습니다. 핵심 차이는 "LLM이 그 사이에 끼는가"입니다.

- **AGENT(toolsEnabled)**: LLM이 스스로 "이 상황에 어떤 tool을, 어떤 인자로, 몇 번 부를지" 판단합니다. 도구 선택이 열려있는 탐색적 작업(예: "필요하면 알아서 DB를 조회해서 답해줘")에 맞습니다. 대신 호출 여부/인자/횟수 모두 비결정적이고, LLM 호출 비용·지연이 매번 붙습니다.
- **TOOL**: 이미 "이 step에선 정확히 이 Tool을 이 인자로 호출한다"는 게 정해진 경우입니다. `inputTemplate`으로 인자를 결정론적으로 만들고, LLM 없이 바로 호출하고, 성공/실패도 Tool의 반환값으로 결정론적으로 판정합니다.

TOOL step을 없애고 AGENT+tool-calling으로 대체하면, 바로 지난번에 짚었던 문제가 그대로 재발합니다 — LLM이 tool 호출 결과를 자기 말로 요약/재서술하면서 코드펜스나 `[검증 결과]` 같은 레이블을 붙이는 걸 강제로 막을 방법이 없고(`ToolStepRunner.stripLlmArtifacts`가 방어하는 게 정확히 이 케이스입니다), 성공/실패 판정도 다시 "LLM이 만든 텍스트를 사람이 해석"하는 문제로 돌아갑니다. 즉 TOOL step은 우회로가 아니라 "결정론이 필요한 지점에서 LLM을 일부러 빼는" 설계 의도로 봐야 합니다.

실무적으로 걸리는 건 오히려 다른 쪽입니다: 지금 YAML 작성자 입장에서 "이 step은 AGENT+tool로 갈지 TOOL로 갈지"를 판단할 기준이 문서화돼 있지 않습니다. 필요하시면 `docs/09.dstone-ai-engine.md`에 "결정론적 검증/필수 호출이면 TOOL, 판단이 필요한 호출이면 AGENT"라는 선택 기준을 한 단락 추가해드릴까요?

---

## 질문

응 일단 앞서 답변한 1번 2번 에 대한 수정을 진행해줘

---

## 답변

1)·2) 아래에서 언급한 항목이 꽤 여러 개라, 어디까지 진행할지 확인하고 시작하겠습니다. 특히 다지 분기/동적 팬아웃은 제가 "지금 당장은 안 만들어도 된다"고 이미 말씀드렸던 항목이라 명시적으로 여쭤봅니다.

---

## 답변

Now I have enough context. Let me implement all four changes, starting with the new status records.

---

## 답변

Now update `StepOutput` to add the `route` field and new factories:

---

## 답변

Now `StepType` — add `ROUTER`:

---

## 답변

Now `StepDefinition` — add `structuredOutput`, `routes`, `forEachVariable`, `itemVariable`:

---

## 답변

Good, that's consistent. Now update `ToolStepRunner` to use `ToolOutcome` with legacy fallback:

---

## 답변

Now update `SqlSyntaxTool` to demonstrate the structured `ToolOutcome` convention:

---

## 답변

Now the big piece — `WorkFlowExecutor`. Let me rewrite the relevant sections: namespacing, ROUTER routing, and dynamic `forEach` fan-out.

---

## 답변

Now update the `decideTransition` call site to pass `route`:

---

## 답변

Now update `runOne` and `runGroup` for namespacing + route, and add `runForEach`:

---

## 답변

Now let's update `runnerFor` for ROUTER and rewrite `decideTransition` to support ROUTER routing, plus refactor the shared "resolve next id" logic:

---

## 답변

Now update the `GroupResult` record to add `route`:

---

## 답변

Now `WorkFlowRegistry` validation for the new combinations:

---

## 답변

Now let's build to catch compile errors. First `dstone-common`, then `dstone-ai-engine`:

---

## 답변

dstone-common built cleanly. Now compile dstone-ai-engine:

---

## 답변

Compiles clean. Let's check for test sources that might reference the old `StepOutput`/`decideTransition` signatures, and also run full `mvn test-compile`:

---

## 답변

Now let's check the docs file for the sections I need to update:

---

## 답변

Now update section 5 (Workflow 실행 모델) — success/failure convention and add ROUTER/forEach/namespacing paragraphs:

---

## 답변

Good, reads coherently. Now update section 9 (YAML 작성법) to show ROUTER/forEach/structuredOutput syntax:

---

## 답변

Still compiles clean. Let me review the full `WorkFlowExecutor.java` once more end-to-end and `ToolStepRunner.java` for correctness before wrapping up:

---

## 답변

This is a critical correctness risk I need to verify before calling this done — Spring AI's `PromptTemplate` uses StringTemplate (ST4) under the hood, which historically disallows dots in attribute names. Since namespaced `data` keys now flow into `variables`, and `AgentExecutor.buildSpec` passes the **whole** variables map into `PromptTemplate.render(variables)` for every AGENT/SUPERVISOR/ROUTER step's system prompt, this could throw at runtime. Let me check.

---

## 답변

2.0.1. Let's disassemble that jar's `StTemplateRenderer`:

---

## 답변

Confirmed via bytecode: `StTemplateRenderer.apply()` unconditionally calls `ST.add(key, value)` for **every** entry in the variables map, and `ST.add()` throws `IllegalArgumentException("cannot have '.' in attribute names")` for any dotted key. Since my namespacing change puts dotted keys (`stepId.field`) into the shared `variables` map, **every subsequent AGENT/SUPERVISOR/ROUTER step's system-prompt rendering would crash** the moment any prior step produces structured data — this would break the very features I just added. I need to fix `AgentExecutor` before this is done.

---

## 답변

Now update the docs/javadoc to accurately reflect this constraint. First `StepOutput`/`StepPayload`:

---

## 답변

Now update `WorkFlowExecutor`'s class-level javadoc footnote and the docs file:

---

## 답변

Now the docs file — fix the "data 네임스페이싱" paragraph and the `structuredOutput` YAML example (which showed the token in a TOOL step already, correctly — but let me verify and add the explicit ST caveat):

---

## 답변

Now add this fix to the changelog table I added earlier:

---

## 답변

Now let's recompile to make sure everything still builds after this fix:

---

## 답변

Let's do a final full read-through of `WorkFlowExecutor.java` to catch anything else before wrapping up:

---

## 답변

Let's view the rest of the file to check `run()`'s dispatch section and confirm the `route` plumbing is complete end-to-end:

---

## 답변

Everything is consistent and compiles clean. Let's do a final full `mvn package -DskipTests` to make sure packaging (not just compile) succeeds, then wrap up:

---

## 답변

Clean diff — exactly the files expected, build passes. Summary of what was implemented:

**1. 구조화 AGENT 출력** — `StepDefinition.structuredOutput: true`면 AGENT step이 자유 텍스트 대신 `StepPayload(primaryText, data)`로 응답받음(`AgentExecutor.callForEntity` 신설, `Verdict`/`RouteDecision`/`StepPayload` 공통 통로).

**2. data 네임스페이싱** — `WorkFlowExecutor.namespaced()`가 모든 `StepOutput.data()`를 `{stepId.키}`로 붙여 병합. 구현 중 **런타임 크래시 버그를 하나 발견해서 같이 고쳤습니다**: Spring AI의 `PromptTemplate`(ST4 기반)은 속성 이름에 점(`.`)을 허용하지 않는데, 네임스페이싱된 키가 variables에 섞이면 그 토큰을 전혀 안 쓰는 Agent의 prompt 렌더링까지 예외로 깨지는 걸 바이트코드로 확인했습니다. `AgentExecutor.promptSafeVariables()`로 prompt 렌더링 직전에 필터링해서 막았고, `{stepId.키}`는 TOOL step의 `inputTemplate`에서만 쓸 수 있다는 제약을 문서화했습니다.

**3. TOOL 성공/실패 개선** — `ToolOutcome(success, message)` 구조화 값을 우선 쓰고, 안 주면 기존 `"실패:"` 접두사로 하위호환. `SqlSyntaxTool`을 레퍼런스로 마이그레이션.

**4. 다지 분기 + 동적 팬아웃** — `StepType.ROUTER`(+`routes` 맵)와 `StepDefinition.forEachVariable`(+`itemVariable`) 추가. `WorkFlowRegistry`에 조합 검증(APPROVAL/ROUTER는 parallelGroup 불가, forEach는 parallelGroup과 병행 불가) 추가.

`docs/09.dstone-ai-engine.md`도 실행 모델/YAML 작성법/변경 이력 섹션을 갱신했습니다. `mvn package`까지 정상 통과합니다.
