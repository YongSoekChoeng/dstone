/**
 * 토큰 사용량/비용/트레이싱/평가(Eval) 결과 로깅(Phase 4). 현재 구현된 것은
 * {@link net.dstone.ai.observability.usage}(토큰 사용량/비용/지연시간 로깅)뿐이다 - Eval 결과 로깅은
 * 평가 데이터셋/채점 로직 등 이 모듈에 없는 전제가 먼저 필요해 아직 손대지 않았다.
 */
package net.dstone.ai.observability;
