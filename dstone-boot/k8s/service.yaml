apiVersion: v1
kind: Service
metadata:
  name: dstone-boot
  namespace: dstone
spec:
  type: NodePort
  selector:
    app: dstone-boot
  ports:
    - port: 7081
      targetPort: 7081
      nodePort: 30081
